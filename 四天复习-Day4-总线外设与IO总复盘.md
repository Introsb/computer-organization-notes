# 四天复习 Day 4：总线、外设、I/O 与总复盘

> [!summary] 今日主线
> 今天把计算机系统“接起来”。  
> 第 7 章讲部件之间如何共享通路；第 8 章讲外部设备本身；第 9 章讲主机如何控制外设输入输出。最后做全书总复盘。

覆盖章节：

- 第 7 章：总线
- 第 8 章：外部设备
- 第 9 章：输入输出系统
- 全书串联复盘

---

## 0. 今日时间安排

| 时间块 | 任务 | 产出 |
|---|---|---|
| 80 min | 第 7 章总线概念、仲裁、定时 | 会比较仲裁和定时方式 |
| 90 min | 第 8 章磁盘、RAID、显示 | 会做磁盘和显存计算 |
| 90 min | 第 9 章 I/O 控制方式 | 会比较查询/中断/DMA/通道 |
| 60 min | 全书高频题型回收 | 完成总表 |
| 60 min | 闭卷自测 | 找最后漏洞 |

---

## 1. 第 7 章：总线

### 1.1 总线核心

```text
总线 = 多个部件共享的一组公共信息传输线
```

三类信号：

| 总线 | 传送内容 |
|---|---|
| 地址总线 | 地址 |
| 数据总线 | 数据 |
| 控制总线 | 读写、中断、请求、响应等控制信号 |

> [!warning] 易错
> 总线共享不等于多个设备同时发送。任一时刻通常只能一个设备驱动总线。

### 1.2 三态门

```text
0
1
高阻态 Z
```

高阻态相当于“断开”，用于防止多个设备同时驱动总线。

### 1.3 总线事务和总线周期

典型总线周期：

```text
申请分配
寻址
传送
结束
```

### 1.4 总线结构

| 结构 | 特点 |
|---|---|
| 单总线 | 结构简单，瓶颈明显 |
| 双总线 | 主存总线和 I/O 总线分开，性能提高 |
| 三总线 | CPU、主存、I/O 通路更细分，性能更好 |

### 1.5 总线仲裁

| 仲裁方式 | 优点 | 缺点 |
|---|---|---|
| 链式查询 | 硬件简单 | 优先级固定，离仲裁器近的优先 |
| 计数器定时查询 | 优先级可变 | 控制比链式复杂 |
| 独立请求 | 响应最快 | 硬件线数多，成本高 |

### 1.6 总线定时

| 定时方式 | 特点 |
|---|---|
| 同步定时 | 统一时钟，速度快，适合速度接近部件 |
| 异步定时 | 请求/应答握手，可靠，适合速度差异大部件 |
| 半同步 | 同步基础上允许等待 |
| 分离式 | 请求和响应分离，提高总线利用率 |

异步三种互锁：

```text
不互锁：双方不等对方撤销
半互锁：一方等待另一方响应
全互锁：请求和响应都互相等待撤销
```

### 1.7 总线带宽

```text
总线带宽 = 总线宽度 × 总线频率
```

注意 bit 和 Byte 换算：

```text
8 bit = 1 Byte
```

---

## 2. 第 8 章：外部设备

### 2.1 外设分类

| 类型 | 例子 |
|---|---|
| 输入输出设备 | 键盘、鼠标、显示器、打印机 |
| 辅助存储器 | HDD、SSD、U盘、光盘、磁带 |
| 终端设备 | ATM、POS、自助售票机 |
| 过程控制设备 | ADC、DAC、传感器、执行器 |

### 2.2 磁盘结构

核心概念：

```text
盘片
磁道
扇区
柱面
磁头
```

柱面：

```text
所有盘面上半径相同的磁道集合
```

### 2.3 磁盘容量

常见公式：

```text
磁盘容量 = 盘面数 × 每面磁道数 × 每磁道扇区数 × 每扇区字节数
```

### 2.4 磁盘平均存取时间

```text
平均存取时间 = 平均寻道时间 + 平均旋转等待时间 + 数据传输时间
```

平均旋转等待时间：

```text
平均旋转等待时间 = 旋转一周时间 / 2
```

数据传输率：

```text
数据传输率 = 每转数据量 × 转速
```

### 2.5 RAID

| RAID | 核心 | 特点 |
|---|---|---|
| RAID 0 | 条带化 | 提高速度，无冗余 |
| RAID 1 | 镜像 | 可靠性高，容量利用率低 |
| RAID 5 | 分布式奇偶校验 | 速度、容量、可靠性折中 |

### 2.6 显示和显存

显存容量：

```text
显存容量 = 分辨率横向像素 × 纵向像素 × 每像素位数
```

换成字节：

```text
bit / 8 = Byte
```

> [!warning] 易错
> 颜色数 = 2^(每像素位数)。  
> 不是用颜色数直接乘像素后就结束，通常还要换算 bit / Byte。

---

## 3. 第 9 章：输入输出系统

### 3.1 I/O 接口

```text
接口 = 主机和外设的交接界面
端口 = 接口中 CPU 可直接访问的寄存器
```

三类端口：

```text
数据端口
命令端口
状态端口
```

端口寻址：

| 方式 | 特点 |
|---|---|
| 独立编址 | I/O 地址和主存地址分开，用 IN/OUT |
| 统一编址 | I/O 端口和主存统一编址，用普通访存指令 |

### 3.2 四种 I/O 控制方式

| 方式 | 谁搬数据 | CPU 被打扰程度 | 适用 |
|---|---|---|---|
| 程序查询 | CPU 程序 | 一直等待 | 简单低速外设 |
| 程序中断 | CPU 中断服务程序 | 准备好后打扰 | 中低速外设 |
| DMA | DMA 控制器 | 开始和结束参与 | 高速数据块 |
| 通道 | 通道处理器 | 启动和结束参与 | 大量外设并行 I/O |

### 3.3 中断

CPU 响应中断条件：

```text
有请求
CPU 接收到请求
EINT = 1
未屏蔽
优先级通过
当前指令执行完毕
```

中断隐指令：

```text
保存断点
关中断
识别中断源 / 形成入口地址
转入中断服务程序
```

中断全过程：

```text
中断请求
→ 中断判优
→ 中断响应
→ 中断处理
→ 中断返回
```

### 3.4 DMA

DMA 流程：

```text
CPU 预处理
DMA 控制器传送数据
DMA 结束后中断 CPU 做后处理
```

关键握手：

| 信号 | 含义 |
|---|---|
| DREQ | 外设向 DMA 控制器请求 |
| DACK | DMA 控制器响应外设 |
| HRQ | DMA 控制器向 CPU 请求总线 |
| HLDA | CPU 向 DMA 控制器授权总线 |

DMA 三种传送方法：

```text
CPU 停止访问主存法：DMA 连续占用，CPU 等
存储器分时法：CPU 和 DMA 固定分时访问
周期挪用法：DMA 临时借一个周期传一个数据
```

### 3.5 通道

```text
通道 = 能执行通道程序的 I/O 专用处理器
```

类型：

| 类型 | 适用 |
|---|---|
| 字节多路通道 | 多台低速设备 |
| 选择通道 | 高速设备，一次服务一台 |
| 数组多路通道 | 多台高速设备，兼顾分时和成组传送 |

---

## 4. 全书总复盘

### 4.1 全书主线

```text
第1章：计算机系统整体结构
第2章：数据如何表示
第3章：指令如何表达操作
第4章：运算如何实现
第5章：数据如何存储
第6章：CPU 如何执行指令
第7章：部件如何通过总线连接
第8章：外部设备如何工作
第9章：主机如何控制外设 I/O
```

### 4.2 最容易连起来考的线

#### 指令执行线

```text
机器指令格式
→ 寻址方式形成 EA
→ CPU 取指/译码/执行
→ 微操作和控制信号
→ 访问主存或外设
```

#### 存储访问线

```text
CPU 发地址
→ Cache 查找
→ 主存访问
→ 必要时虚拟地址变换
→ 数据经总线传回 CPU
```

#### I/O 传送线

```text
外设
→ 接口/端口
→ 总线
→ CPU / DMA / 通道
→ 主存
```

---

## 5. 最后一轮闭卷自测

### 必会简答

1. 冯·诺依曼计算机的基本特点是什么？
2. 原码、反码、补码的区别是什么？
3. 扩展操作码的核心思想是什么？
4. 寻址方式中 A、EA、S 分别是什么？
5. Cache 三种映像方式有什么区别？
6. CPU 取指周期微操作是什么？
7. 总线仲裁为什么必要？三种集中仲裁方式怎么比较？
8. 磁盘平均存取时间由哪几部分组成？
9. 程序查询、中断、DMA、通道四种 I/O 方式怎么比较？
10. RISC 为什么适合流水线？

### 必会计算

1. 补码真值计算。
2. IEEE754 单精度转换。
3. 溢出判断。
4. 主存芯片容量扩展。
5. Cache 地址字段划分。
6. 磁盘容量与平均存取时间。
7. 显存容量。
8. 总线带宽。

---

## 6. 考前最后 30 分钟

只看这些：

```text
第2章：10000000、补码权值、补码小数、IEEE754
第3章：扩展操作码、寻址公式、堆栈、CALL/JMP/RET
第4章：溢出判断、浮点加减五步法
第5章：芯片扩展、Cache 映像、虚拟存储
第6章：取指微操作、微程序层级、流水线相关
第7章：仲裁、定时、总线带宽
第8章：磁盘公式、RAID、显存容量
第9章：中断隐指令、DMA、通道
```

---

## 7. 今晚复盘

```text
全书最稳的章节：
1.
2.

还需要临考前再看的章节：
1.
2.

最可能丢分的题型：
1.
2.
3.
```
