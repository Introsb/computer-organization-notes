# 第六章 中央处理器

> [!summary] 本章主线
> 中央处理器这一章的核心问题不是“CPU 有哪些部件”，而是：
> **一条机器指令如何在 CPU 内部被取出、译码、执行？**
>
> CPU 的本质是：控制器在正确的时间发出控制信号，使数据沿着数据通路在 PC、MAR、MDR、IR、ACC、ALU、主存之间流动，最终完成机器指令规定的操作。

本章核心问题：

1. CPU 内部有哪些寄存器？各起什么作用？
2. 控制器的三大实现方法是什么？组合逻辑和微程序各自优劣？
3. 指令周期、机器周期、节拍、工作脉冲之间是什么关系？
4. 微程序控制的基本原理：微命令、微操作、微指令、微程序之间的关系
5. 流水线为何能提高吞吐率？相关性如何破坏流水线？
6. RISC 和 CISC 的本质区别是什么？

---

## 学习目标

| 层次 | 内容 |
|---|---|
| ==掌握== | 取指周期的微操作序列（公共操作）、微程序控制器组成与工作过程、微程序入口地址和后继微地址的形成 |
| ==理解== | CPU 的功能和主要寄存器、控制器的基本组成、指令周期/机器周期的概念、指令执行的基本过程 |
| ==了解== | 不同控制方式（同步/异步/联合）、控制单元的设计、流水线技术和 RISC 技术 |

---

## 6.1 中央处理器的功能和组成

### 6.1.1 CPU 的功能

CPU 的基本功能就是对**指令流**和**数据流**在时间与空间上实施正确的控制。

> 指令流是 CPU 执行的指令序列，数据流是根据指令操作要求依次存取数据的序列。对于冯·诺依曼结构，数据流是由指令流来**驱动**的。

### 6.1.2 CPU 中的主要寄存器

#### 1. 通用寄存器

存放原始数据和运算结果，有的可充当变址寄存器、计数器、地址指针等。少则几个，多则上百个，由程序编址访问。

**累加寄存器 ACC**：暂时存放 ALU 运算的结果。运算器中==至少有一个累加寄存器==。

#### 2. 专用寄存器

| 寄存器 | 符号 | 功能 |
|---|---|---|
| **程序计数器** | PC | 存放下一条指令地址；顺序执行时自动 +1；转移时将目标地址送入 |
| **指令寄存器** | IR | 存放从主存取出的当前指令，执行期间内容不变 |
| **存储器数据寄存器** | MDR | CPU 与主存之间的数据缓冲（双向） |
| **存储器地址寄存器** | MAR | 保存当前 CPU 访问的主存单元地址 |
| **程序状态字寄存器** | PSWR | 存放 PSW：① 状态标志（C/Z/V 等）；② 控制标志（中断、陷阱） |

#### 从一条指令理解寄存器分工

以 `ADD X` 为例，软件视角下它表示：

```text
ACC ← ACC + M[X]
```

但硬件执行时需要多个寄存器配合：

1. `PC` 给出当前指令地址
2. `MAR` 接收地址，用于访问主存
3. `MDR` 暂存从主存读出的指令
4. `IR` 保存当前指令
5. `IR` 中的地址码 X 再送入 `MAR`
6. 主存读出 `M[X]` 到 `MDR`
7. `ACC` 与 `MDR` 的内容送入 ALU 运算
8. 结果写回 `ACC`
9. `PSWR` 根据运算结果更新状态标志

因此：

| 寄存器/部件 | 解决的问题 |
|---|---|
| `PC` | 下一条指令在哪里 |
| `IR` | 当前指令是什么 |
| `MAR` | 访问主存哪个地址 |
| `MDR` | 主存和 CPU 之间传什么数据 |
| `ACC` / `ALU` / `PSWR` | 如何完成运算并记录状态 |

### 6.1.3 CPU 的组成

| 部分 | 主要功能 |
|---|---|
| **控制器** | ① 取指令并指出下条指令位置；② 译码产生操作控制信号；③ 指挥 CPU/主存/IO 之间的数据流动 |
| **运算器** | ① 执行所有算术运算；② 执行所有逻辑运算并进行逻辑测试 |

**CPU 模型核心组件：**

- **运算器部分**：ALU、ACC、PSWR
- **控制器部分**：PC、IR、ID（指令译码器）、CU（控制单元，产生微操作控制信号）
- **存储器接口**：MAR、MDR

### 6.1.4 CPU 的主要技术参数

| 参数 | 说明 |
|---|---|
| **字长** | 单位时间内同时处理的二进制数据位数（8/16/32/64位） |
| **主频（内频）** | CPU 内部工作频率，倒数 = 时钟周期（最小时间单位） |
| **外频** | 主板为 CPU 提供的基准时钟频率 |
| **倍频** | 内频 = 外频 × 倍频 |
| **前端总线频率 FSB** | CPU 与主存/显卡等高速部件交换数据的主通道 |
| **QPI** | 取代 FSB 的点对点高速传输技术，使用 GT/s |
| **DMI** | 连接 PCH 的总线，取代传统北桥 |
| **片内 Cache** | L1（指令/数据分离）、L2、L3 三级缓存 |
| **TDP** | 热设计功耗，散热系统必须能驱散的最大热量 |
| **地址总线宽度** | 决定 CPU 可访问的最大物理地址空间 |
| **数据总线宽度** | 决定一次数据交换的信息量 |

---

## 6.2 控制器的组成和实现方法

### 6.2.1 控制器的基本组成

控制器的核心是**微操作信号发生器（CU）**，它结合三类信息发出控制信号：

| 输入源 | 提供信息 |
|---|---|
| IR（指令寄存器） | "做什么" — 操作码经 ID 译码 |
| 时序部件（脉冲源 + 节拍发生器） | "什么时候做" — 节拍 T₁、T₂、T₃ |
| 状态与反馈（PSWR + IO/控制台） | "根据什么情况做" — 进位/零标志/中断等 |

**控制器四大部件：**

| 部件 | 组成 | 功能 |
|---|---|---|
| **指令部件** | PC、IR、ID、地址形成部件 | 取指令并分析指令 |
| **时序部件** | 脉冲源、启停控制逻辑、节拍信号发生器 | 产生时序信号，保证各部件有节奏工作 |
| **微操作信号发生器（CU）** | — | 根据不同指令产生不同的微操作序列 |
| **中断控制逻辑** | — | 控制中断处理的硬件逻辑 |

#### 控制器的本质

控制器不是用来计算的，而是用来发出控制信号。

例如微操作：

```text
(PC) → MAR
```

在硬件上不是自动发生的，而是控制器发出：

```text
PC_out, MAR_in
```

于是 PC 的内容被送上总线，MAR 从总线接收数据。

因此，控制器要解决三个问题：

1. **当前要做什么？**  
   由 IR 中的操作码决定。

2. **当前什么时候做？**  
   由机器周期、节拍、工作脉冲决定。

3. **当前能不能做 / 该走哪条分支？**  
   由 PSWR、中断请求、I/O 状态等反馈决定。

### 6.2.2 控制器的硬件实现方法

| 类型 | 原理 | 优点 | 缺点 |
|---|---|---|---|
| **组合逻辑型（硬连线）** | 由门电路组成的树形网络 | ==速度最快== | 结构不规整，设计/调试/修改困难，扩展性差 |
| **存储逻辑型（微程序）** | 微操作信号代码化，存入控制存储器 | ==设计规整、易修改扩充、主流方案== | 增加一级控制存储器，速度比组合逻辑慢 |
| **PLA 型（可编程逻辑阵列）** | 结合前两种思想，可编程的组合逻辑 | 克服两者缺点 | — |

---

## 6.3 时序系统与控制方式

### 6.3.1 时序系统

#### 四级时序层次

```text
指令周期
├── 机器周期 1：取指周期
│   ├── T1
│   ├── T2
│   └── T3
├── 机器周期 2：取数周期
│   ├── T1
│   ├── T2
│   └── T3
└── 机器周期 3：执行周期
    ├── T1
    ├── T2
    └── T3
```

理解：

| 层次 | 含义 |
|---|---|
| 指令周期 | 完成一条机器指令的总时间 |
| 机器周期 | 完成一个基本操作的时间，如取指、取数、执行 |
| 节拍 | 一个机器周期内部的分步动作 |
| 工作脉冲 | 节拍末尾真正触发寄存器写入或状态翻转的脉冲 |

> [!warning] 易混点
> 机器周期不是机器指令周期。  
> 一条机器指令通常需要多个机器周期，例如 `ADD X` 可能需要：  
> `取指周期 → 取数周期 → 执行周期`。  
> 每个机器周期内部又由若干节拍组成。

#### 1. 指令周期和机器周期

> [!important] 核心公式
> $$指令周期 = i \times 机器周期$$

| 概念 | 含义 |
|---|---|
| **指令周期** | 从取指令、分析取数到执行完该指令所需的全部时间 |
| **机器周期（CPU周期）** | 每个机器周期完成一个基本操作 |

常见 CPU 周期：取指周期、取数周期、执行周期、中断周期等。

> [!tip] 最短指令周期
> 一般情况下，一条指令所需最短时间为**两个机器周期**：取指周期 + 执行周期。

每个机器周期对应一个**周期状态触发器**，任何时刻有且仅有一个触发器被置 "1"。

#### 2. 节拍

一个机器周期分为若干个相等的时间段，每个时间段对应一个**节拍电位信号**。

| 方法 | 特点 |
|---|---|
| **统一节拍法** | 以最复杂周期为准，所有机器周期长度相等 → 定长 CPU 周期 |
| **分散节拍法** | 按实际需要安排节拍数，各周期长度不同 → 不定长 CPU 周期 |
| **延长节拍法** | 选定基本节拍数，不足时延长 1~2 个节拍 |
| **时钟周期插入** | 微型机常用（如 8086 的 T₁~T₄ + 可插入 T_w） |

#### 3. 工作脉冲

在一个节拍内设置一个或几个工作脉冲，处于节拍的**末尾部分**，保证所有触发器可靠、稳定地翻转。

#### 4. 多级时序系统

```text
指令周期 → 机器周期 M → 节拍 T₁~T₄ → 工作脉冲 p
```

三级间不允许重叠也不允许有空隙，一个接一个准确连接。

### 6.3.2 控制方式

| 方式 | 特点 | 别名 |
|---|---|---|
| **同步控制** | 统一时序信号控制，定长 CPU 周期 | 集中/中央控制 |
| **异步控制** | 各部件自行决定时间，"应答" 方式 | 分散/局部控制 |
| **联合控制** | ==最常用==：部件内部同步，部件之间异步 | — |

### 6.3.3 指令运行的基本过程

| 阶段 | 任务 |
|---|---|
| **取指令阶段** | 公共操作，完成时间 = 取指周期 |
| **分析取数阶段** | ID 识别指令类型，获取操作数（可细分为间址周期、取数周期等） |
| **执行阶段** | 完成指令规定的操作，形成结果并存储（执行周期） |

```text
取指令 → 取数 → 执行指令 → 取下一条...如此周而复始
```

### 6.3.4 指令的微操作序列

> 每条指令被分解为一系列时间上先后有序的**微操作**，微操作序列与 CPU 内部数据通路密切相关。

**符号约定：** `X_in` = 部件 X 的输入开门信号，`X_out` = 部件 X 的输出开门信号。

#### 微操作与控制信号的对应关系

微操作表示 CPU 内部实际发生的数据传送或运算；控制信号是控制器发出的硬件命令。

| 微操作 | 对应控制信号 |
|---|---|
| `(PC) → MAR` | `PC_out, MAR_in` |
| `(MDR) → IR` | `MDR_out, IR_in` |
| `(ACC) → Y` | `ACC_out, Y_in` |
| `(Z) → ACC` | `Z_out, ACC_in` |

> 微操作是“发生了什么”，控制信号是“如何让它发生”。

**单总线结构 CPU 的数据通路特点：**

- 任一时刻只有一个部件向总线发送数据
- 可多个部件同时从总线接收数据
- 需要暂存器 **Y** 和 **Z** 解决单总线 ALU 运算冲突：
  - Y 暂存第一个操作数
  - Z 暂存 ALU 运算结果，避免与总线冲突

#### 单总线结构下微操作能否并行的判断

单总线结构的基本限制：

> 同一时刻只能有一个部件向总线输出，但可以有多个部件同时从总线输入。

| 控制信号组合 | 是否允许 | 原因 |
|---|---|---|
| `PC_out, MAR_in` | 允许 | 一个输出，一个输入 |
| `MDR_out, IR_in, Y_in` | 允许 | 一个输出，多个输入 |
| `PC_out, MDR_out` | 不允许 | 两个部件同时输出，会产生总线冲突 |
| `ACC_out, Y_in, Read` | 通常允许 | CPU 内部传送与主存读控制可并行，只要时序允许 |

判断原则：

1. 看是否有多个 `_out`
2. 如果多个部件同时向总线输出，一般不允许
3. 多个 `_in` 可以同时存在
4. `Read` / `Write` 不等同于总线输出

#### 为什么单总线结构需要 Y 和 Z？

ALU 运算通常需要两个操作数，但单总线一次只能传送一个数据。

例如要完成：

```text
ACC ← ACC + MDR
```

不能直接让 `ACC_out` 和 `MDR_out` 同时有效，因为这会导致总线冲突。

正确做法是分三步：

```text
1. ACC_out, Y_in
   Y ← ACC

2. MDR_out, ALU_add, Z_in
   Z ← Y + MDR

3. Z_out, ACC_in
   ACC ← Z
```

其中：

- `Y` 用来暂存第一个操作数
- `Z` 用来暂存 ALU 运算结果
- 这样可以避免两个源操作数同时争用单总线

#### 示例：取指周期的微操作序列（公共操作）

| 步骤 | 微操作 | 说明 |
|---|---|---|
| ① | `(PC) → MAR` | PC 内容送地址寄存器 |
| ② | `Read` | CU 向主存发读命令 |
| ③ | `M(MAR) → MDR` | 主存读出指令至数据寄存器 |
| ④ | `(MDR) → IR` | 指令送指令寄存器 |
| ⑤ | `(PC) + 1 → PC` | PC 指向下一条指令 |

---

## 6.4 微程序控制原理

> [!note] 核心思想
> 将微操作控制信号按一定规则进行信息编码（代码化），形成控制字（微指令），再按时间先后排列构成微程序，存放在只读的控制存储器中。

### 6.4.1 基本概念

#### 1. 基本术语

| 术语 | 含义 |
|---|---|
| **微命令** | 控制部件向执行部件发出的最基本的控制信号 |
| **微操作** | 微命令对应的具体操作过程（与微命令==一一对应==） |
| **微指令** | 控制存储器一个单元的内容（控制字），若干微命令的集合 |
| **微地址** | 存放控制字的控制存储器单元地址 |
| **微周期** | 读取一条微指令并执行相应微命令所需的全部时间 |
| **微程序** | 一系列微指令的有序集合，一条机器指令对应一段微程序 |

#### 2. 层次关系

```text
微命令 → 微操作（一一对应）
   ↓
微指令 = 一组微命令的集合（同一时钟周期内并行执行）
   ↓
微程序 = 实现一条机器指令的微指令序列
```

#### 微程序控制的层级关系

```text
机器指令：ADD X
    ↓ 由一段微程序实现
微程序：取指微指令 → 取数微指令 → 加法微指令 → 返回取指
    ↓ 每条微指令包含若干微命令
微指令：PC_out, MAR_in, Read
    ↓ 每个微命令控制一个微操作
微操作：(PC) → MAR
```

理解：

- 程序员看到的是机器指令
- 机器设计者关心的是微程序
- 控制存储器 CM 中保存的是微指令序列
- 微指令发出的微命令驱动 CPU 数据通路

#### 机器指令和微指令的区别

| 对比项 | 机器指令 | 微指令 |
|---|---|---|
| 所属层次 | 机器语言层 | 微程序控制层 |
| 存放位置 | 主存 | 控制存储器 CM |
| 执行对象 | CPU 执行一条用户程序指令 | 控制器发出一组微命令 |
| 对程序员是否可见 | 可见 | 通常不可见 |
| 作用 | 表示要完成什么操作 | 表示硬件每一步如何完成 |

#### 3. 微程序 vs 程序

| | 微程序 | 程序 |
|---|---|---|
| 组成 | 微指令 | 机器指令 |
| 存放位置 | ==控制存储器（CM）== | 主存 / 辅存 |
| 编制者 | 计算机设计者 | 软件设计人员 |
| 对用户 | ==透明==（无须知道） | 可见 |

> [!important] 微程序控制的计算机涉及两个层次
> 1. 程序员看到的**机器语言层**
> 2. 机器设计者看到的**微程序层**（微指令、微程序、控制存储器）

### 6.4.2 微指令编码法

#### 1. 直接控制法（不译码法）

- 操作控制字段每一位代表一个微命令
- 位 = 1 → 微命令有效；位 = 0 → 无效
- 优点：简单、并行性强、速度快
- 缺点：==微指令字太长==（N 个微命令需 N 位）

#### 2. 最短编码法

- 所有微命令统一编码，每条微指令只定义一个微命令
- 若微命令总数为 N，则字段长：$L \geq \lceil\log_2 N\rceil$
- 优点：微指令字最短
- 缺点：同一时刻只能产生一个微命令，==无法利用并行性==，需译码器

#### 3. ==字段编码法==（折衷方案，最常用）

将操作控制字段分为若干小段：
- 段内采用最短编码法
- 段与段之间采用直接控制法（独立定义）

**分段原则：**

| 原则 | 说明 |
|---|---|
| 互斥性微命令分在同一段 | 提高信息利用率，缩短指令字长 |
| 兼容性微命令分在不同段 | 充分发挥硬件并行性 |
| 与数据通路结构相适应 | — |
| 每段留出一个不操作状态 | 如 3 位字段最多表示 7 个互斥微命令（000 = 不操作） |

**两种类型：**

| 类型 | 特点 |
|---|---|
| **字段直接编码法**（显式/单重定义） | 各字段独立定义本字段微命令，广泛应用 |
| **字段间接编码法**（隐式/多重定义） | 一个字段的某些编码需与其他字段联合定义，进一步缩短指令字长但增加译码复杂度 |

### 6.4.3 微程序控制器的组成和工作过程

#### 1. 基本组成

| 部件 | 符号 | 功能 |
|---|---|---|
| **控制存储器** | CM | 核心部件，存放微程序 |
| **微指令寄存器** | μIR | 存放从 CM 取出的当前微指令 |
| **微地址形成部件** | — | 产生初始微地址和后继微地址 |
| **微地址寄存器** | μMAR | 接收微地址，为读取 CM 做准备 |

**微指令分为两部分：**
- **操作控制字段**（微操作码）：产生各微操作控制信号，控制硬件部件
- **顺序控制字段**（微地址码）：决定下一条微指令的地址

#### 2. 工作过程

```
① 自动将取指微程序入口地址送 μMAR，从 CM 读取取指微指令
   → 完成取机器指令的公共操作
   → 机器指令存入 IR

② IR 中的操作码通过微地址形成部件
   → 生成该指令对应微程序的入口地址

③ 从 CM 逐条取出微指令并执行

④ 执行完毕后回到取指微程序入口，取下一条机器指令
   如此周而复始
```

> CM 中的微程序数 = 机器指令数 + 取指/间址/中断等公用微程序数

### 6.4.4 微程序入口地址的形成

| 方法 | 适用场景 |
|---|---|
| **一级功能转换** | 操作码字段位数和位置固定 → 操作码直接对应入口地址 |
| **二级功能转换** | 同类指令操作码固定，不同类指令操作码不固定 → 先分类，再按操作码区分 |
| **PLA 电路转换** | 操作码位数和位置都不固定 → PLA 将操作码翻译成入口地址 |

### 6.4.5 后继微地址的形成

| 方式 | 原理 | 特点 |
|---|---|---|
| **增量方式（顺序—转移型）** | 顺序执行时 μPC+1，转移时由顺序控制字段产生 | 简单，但不能实现多路并行转移 |
| **断定方式** | 顺序控制字段由非测试段 + 测试段组成，根据测试结果决定下址 | 测试段 n 位可产生 $2^n$ 路分支，灵活 |

### 6.4.6 微程序设计

#### 微指令类型

| 类型 | 特点 | 优缺点 | 别名 |
|---|---|---|---|
| **水平型** | 一次能定义并执行多个微命令，并行性强 | 微指令数少、速度快，但指令字长、难掌握 | 硬方法 |
| **垂直型** | 一次只执行一个微命令 | 指令字短、易掌握，但微指令数多、速度慢 | 软方法 |
| **混合型** | 兼顾两者 | 指令字不太长，有一定并行能力 | — |

#### 微指令运行方式

| 方式 | 原理 | 特点 |
|---|---|---|
| **串行** | 取微指令 → 执行 → 取下一条，严格交替 | 控制简单但速度慢，CM 和数据通路利用率低 |
| **重叠（并行）** | 执行第 N 条时，同时用顺序控制字段取第 N+1 条 | ==速度极大提高==，消除串行等待时间，但硬件复杂 |

---

## 6.5 控制单元的设计

### 6.5.1 简单的 CPU 模型

指令系统示例：

| 类型 | 指令 |
|---|---|
| **非访存指令** | CLA（清 ACC）、COM（取反）、INC（加1）、SHR（算术右移）、CSL（循环左移）、STP（停机） |
| **访存指令** | ADD、SUB、AND、LDA（取数）、STA（存数） |
| **转移指令** | JMP（无条件）、JZ（零转）、JN（负转）、JC（进位转） |

### 6.5.2 组合逻辑控制单元设计

**微操作节拍安排原则：**
1. 注意微操作的先后顺序（不可颠倒）
2. 控制对象不同的微操作尽量安排在同一节拍
3. 耗时短的微操作可安排在同一节拍（允许先后次序）

**取指周期节拍安排（3 节拍）：**

| 节拍 | 微操作 |
|---|---|
| T₁ | `(PC) → MAR`，Read |
| T₂ | `M(MAR) → MDR`，`(PC) + 1 → PC` |
| T₃ | `(MDR) → IR`，`OP(IR) → ID`（译码） |

**设计步骤：**
1. 列出微操作命令的操作时间表
2. 进行微操作信号综合（写出各控制信号的逻辑表达式）
3. 画出微操作命令的逻辑电路图

### 6.5.3 微程序控制单元设计

**设计步骤：**
1. 确定微程序控制方式（根据速度要求）
2. 拟定微命令系统 + 设计微指令格式（字段划分、编码、地址形成）
3. 编制微程序并反复核对/修改
4. 将微程序代码化（转换为二进制代码）
5. 写入控制存储器对应单元

---

## 6.6 流水线技术

> [!note]
> 指令执行的控制方式：顺序方式 → 重叠方式 → 先行控制 → 流水线控制。

### 6.6.1 重叠控制

设取指/分析/执行每阶段耗时 $t$，$n$ 条指令：

| 方式 | 执行时间 | 说明 |
|---|---|---|
| 顺序 | $T = 3nt$ | 串行执行 |
| 一次重叠 | $T = (2n+1)t$ | "取指 K+1" 与 "执行 K" 重叠 |
| 二次重叠 | $T = (2+n)t$ | "取指 K+2" + "分析 K+1" + "执行 K" 重叠 |

### 6.6.2 先行控制原理

当各指令的 "分析" 与 "执行" 时间不等时，重叠会出现等待。先行控制通过设置**先行缓冲栈**来解决操作数提前准备和结果缓冲的问题，是重叠控制的改进。

### 6.6.3 流水工作原理

#### 1. 流水线概念

将一个复杂处理过程分成 $m$ 个复杂程度相当、处理时间大致相等的**子过程**，每个子过程由独立功能部件完成。同一时间 $m$ 个部件同时进行不同操作。

> 类似工厂流水线：每个 $\Delta t$ 流出一个产品，而生产一个产品的总时间远大于 $\Delta t$。

#### 流水线提高的到底是什么？

流水线并不一定缩短单条指令从开始到结束的总时间。

它提高的是：

> 单位时间内完成的指令数量，即吞吐率。

例如一个三段流水线：

```text
取指 → 译码 → 执行
```

理想情况下，第一条指令仍然需要经过三个阶段；但当流水线填满后，每个节拍都可以完成一条指令。

#### 2. 流水线分类

| 分类维度 | 类型 | 特点 |
|---|---|---|
| **按处理级别** | 操作部件级 / 指令级 / 处理机级（宏流水线） | — |
| **按功能** | 单功能流水线 / 多功能流水线 | — |
| **按工作方式** | 静态流水线（排空前不能切换功能）/ 动态流水线（可同时连接不同功能子集） | 动态必是多功能 |
| **按结构** | 线性流水线（无反馈）/ 非线性流水线（有反馈回路，常用于递归） | — |

#### 3. 指令流水线的相关性

| 类型 | 含义 | 例子 |
|---|---|---|
| **结构相关（资源相关）** | 同一时刻争抢同一资源 | 取指和取数同时访问主存 |
| **数据相关** | 后续指令要用前面操作的结果，但结果尚未产生 | RAW（写后读）、WAR（读后写）、WAW（写后写） |
| **控制相关（指令相关）** | 转移指令导致流水线断流 | JMP / JZ 后的指令可能不该执行 |

#### 三类相关性的直觉理解

| 相关类型 | 直觉 | 例子 |
|---|---|---|
| 结构相关 | 硬件资源不够用 | 取指和取数同时访问同一主存 |
| 数据相关 | 结果还没算出来，后面已经要用 | 后一条指令读取前一条指令的结果 |
| 控制相关 | 下一条指令地址还不确定 | 条件转移指令改变 PC |

---

## 6.7 精简指令系统计算机（RISC）

### 6.7.1 RISC 的特点和优势

#### 主要特点

| 特点 | 说明 |
|---|---|
| 指令总数少 | 一般不超过 100 条 |
| 寻址方式少 | 一般 2~3 种 |
| 指令格式少且长度一致 | 一般 2~3 种格式 |
| 单周期指令 | 除 Load/Store 外大部分在单周期内完成 |
| ==Load/Store 架构== | 只有取数/存数指令访存，其余在寄存器之间操作 |
| 通用寄存器多 | 32 个以上，有的可达上千个 |
| 硬连线控制 | 绝大多数用硬连线实现，不用或少用微程序 |
| 优化编译 | 力求以简单方式支持高级语言 |

#### RISC 的优势

$$t = I \times C \times T$$

| 参数 | RISC vs CISC |
|---|---|
| I（指令数） | RISC 多 20%~40%（指令简单，需更多指令完成同样任务） |
| C（平均周期数） | RISC ==小得多==（大多数指令单周期） |
| T（周期时间） | RISC ==更短==（数据通路简单） |

> 综合：RISC 处理速度比同规模 CISC 提高 **3~5 倍**。

#### 为什么 RISC 适合流水线？

RISC 的设计目标不是单纯“指令少”，而是让指令更加规整，从而更容易流水化。

具体来说：

1. 指令长度固定  
   → 取指和译码更简单

2. 指令格式少  
   → 控制逻辑更规整

3. 寻址方式少  
   → 执行阶段更容易统一

4. Load/Store 架构  
   → 访存操作集中在 Load/Store 指令中，普通 ALU 指令只在寄存器之间操作

5. 多数指令单周期完成  
   → 各流水段时间更容易均衡

因此，RISC 的核心优势不是“少”，而是：

> 简单、规整、容易硬连线控制、容易流水线化。

### 6.7.2 RISC 基本技术

#### 1. 重叠寄存器窗口技术

- 在 CPU 芯片上设置大量寄存器堆
- 将寄存器堆划分为多个窗口
- 每个过程使用相邻的 3 个窗口 + 1 个公共窗口
- 与前后过程各共享 1 个窗口，减少参数传递时的访存

#### 2. 流水线技术

RISC 中流水线势在必行——单周期完成取指/译码/执行/写回需要流水线支持。

#### 3. 延时转移技术

- 遇到转移指令时，编译程序在转移指令后插入空指令（NOP）延迟后继指令进入流水线
- **优化延迟转移**：将转移指令与前条指令调换位置，让流水线不浪费周期

---

## 6.8 微处理器中的新技术

### 6.8.1 超标量和超流水线

| 技术 | 原理 | 特点 |
|---|---|---|
| **超标量** | 重复设置多个功能部件同时工作 | 一个时钟周期内可同时发射多条指令 |
| **超流水线** | 将流水线段进一步细分（段数 ≥ 8） | 一个时钟周期内可分时发射多条指令 |

### 6.8.2 EPIC 的指令级并行处理

- EPIC（显式并行指令代码）：Itanium 挑战 RISC 的基础
- 编译器分析指令依赖关系，将无依赖的 3 条指令组合成 128 位指令束
- 低端 CPU 每周期调度 1 个指令束；高端可调度多个

### 6.8.3 超线程技术（HT）

- Intel 提出：将一个物理 CPU 当两个逻辑 CPU 使用
- 利用特殊硬件指令，允许单个处理器同时执行多重线程
- 减少 CPU 闲置时间，提高运行效率

### 6.8.4 双核与多核技术

| 技术 | 本质 |
|---|---|
| **双核/多核** | "物理" 方式：一个处理器上集成多个运算核心 |
| **超线程** | "逻辑" 方式：模拟两个逻辑处理器 |

---
