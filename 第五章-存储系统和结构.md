# 第五章 存储系统和结构

> [!summary] 章节概览
> 存储系统由多个容量、速度和价格各不相同的存储器构成。本章重点讨论主存储器的工作原理、组成方式以及运用半导体存储芯片组成主存储器的方法，同时介绍高速缓冲存储器（Cache）和虚拟存储器的基本原理。

---

## 学习目标

| 层次 | 内容 |
|---|---|
| ==了解== | 存储器分类方法、存储系统层次结构、SRAM/DRAM基本存储原理、各种ROM、芯片基本结构 |
| ==理解== | 主存基本结构与存储单元、动态RAM刷新、主存与CPU的软连接、Cache存储系统、虚拟存储器 |
| ==掌握== | 主存容量的各种扩展方法 |

---

## 5.1 存储系统的组成

### 5.1.1 存储器分类

#### 1. 按在计算机系统中的作用分类

| 类型 | 特点 | 用途 |
|---|---|---|
| **高速缓冲存储器（Cache）** | 速度匹配CPU，容量小，价格高 | 存放正在执行的程序段和数据 |
| **主存储器（主存）** | CPU可直接随机读写 | 存放运行期间所需的程序和数据 |
| **辅助存储器（辅存）** | 设在主机外部，CPU不能直接访问（需调入主存） | 存放暂不运行的程序和需永久保存的数据 |

#### 2. 按存取方式分类

| 类型 | 特点 |
|---|---|
| **随机存取存储器（RAM）** | 任一单元读写时间相同，与物理位置无关 |
| **只读存储器（ROM）** | 只能读出不能写入，存放不变信息 |
| **顺序存取存储器（SAM）** | 按顺序存取，存取时间与物理位置有关 |
| **直接存取存储器（DAM）** | 介于RAM和SAM之间（先定位区域，再顺序检索） |

#### 3. 按存储介质分类

| 介质 | 特点 |
|---|---|
| **磁芯存储器** | 信息可长期保存，但**破坏性读出**（读后全部变0，需重写） |
| **半导体存储器** | MOS型（集成度高、功耗低、慢）\| 双极型（速度快、集成度低、功耗大） |
| **磁表面存储器** | 磁盘、磁带等，容量大、价格低、速度慢 |
| **光存储器** | 只读/一次写入/可读写，容量很大 |

#### 4. 按信息可保存性分类

- **易失性存储器**：断电后信息消失（如半导体RAM）
- **非易失性存储器**：断电后信息保留（如ROM、磁表面）
- **破坏性读出**：读后原信息被破坏，需紧接**重写（再生）**操作

---

### 5.1.2 存储系统层次结构

> [!important] 设计目标
> 解决**容量、速度、价格**三者之间的矛盾。将不同存储器按体系结构组织为一个统一整体。

**多级存储层次特性：**

| 层级 | 速度 | 容量 | 位价格 |
|---|---|---|---|
| M₁（最近CPU） | 最快 | 最小 | 最高 |
| ... | ... | ... | ... |
| Mₙ（最远CPU） | 最慢 | 最大 | 最低 |

整个系统具有：接近 M₁ 的**速度**，接近 Mₙ 的**容量**，接近 Mₙ 的**位价格**。

**命中率与等效访问时间：**

$$
H = \frac{N_1}{N_1 + N_2}
$$

- $H$：命中率（CPU能在M₁中访问到的概率）
- $N_1$：访问M₁命中次数
- $N_2$：访问M₂的次数

若 M₁ 和 M₂ 同时启动：

$$
T_A = H \times T_{A1} + (1-H) \times T_{A2}
$$

---

#### Cache-主存层次（Cache存储系统）

- 目的：解决**主存速度不足**
- 容量 = 主存容量，价格 ≈ 主存价格
- ==全部用硬件调度==，对系统程序员和应用程序员都**透明**

#### 主存-辅存层次（虚拟存储系统）

- 目的：解决**主存容量不足**
- 速度 ≈ 主存，容量 = 虚拟地址空间，价格 ≈ 辅存
- 由**硬件 + 操作系统**调度，对系统程序员**不透明**，对应用程序员透明

---

## 5.2 主存储器的组织

### 5.2.1 主存储器的基本结构

主存由以下部分组成：

| 部件 | 功能 |
|---|---|
| **存储体** | 核心，存放程序和数据 |
| **地址译码驱动电路** | 译码器 + 驱动器：将地址码转换为译码输出信号，选中存储单元 |
| **I/O和读写电路** | 读出放大器、写入电路、读写控制电路 |

### 5.2.2 主存储器的存储单元

> [!note] 基本概念
> - **位（bit）**：存储信息的最小单位
> - **存储字**：作为一个整体存入或取出的二进制数
> - **存储单元（主存单元）**：存放存储字/存储字节的空间
> - **存储体**：大量存储单元的集合，每个单元有唯一的地址（一一对应）

**两种字节编址方案：**

| 方案       | 代表机型    | 特点                       |
| -------- | ------- | ------------------------ |
| **大端方案** | IBM 370 | 字地址 = 最高有效字节地址，字地址=4的整数倍 |
| **小端方案** | PDP-11  | 字地址 = 最低有效字节地址，字地址=2的整数倍 |


### 5.2.3 主存储器的主要技术指标

#### 1. 存储容量
- 字节编址 → 以字节数表示
- 字编址 → 字数 × 字长
- 如：64K×16 = 128KB

#### 2. 存取速度

| 指标             | 含义                                |
| -------------- | --------------------------------- |
| **存取时间 $T_a$** | 从启动一次操作到完成的时间                     |
| **存取周期 $T_m$** | 连续两次访问间的最短时间（$T_m > T_a$，因需恢复时间）  |
| **主存带宽 $B_m$** | $B_m$ = 主存等效工作频率 × 主存位宽 ÷ 8（字节/秒） |

> [!warning] 破坏性读出RAM
> 存取周期可达存取时间的2倍（$T_m = 2T_a$），因为读出后需马上重写。

#### 3. 可靠性 & 功耗
- 可靠性：用**平均无故障时间 MTBF** 衡量
- 功耗：大多数半导体存储器维持功耗 << 工作功耗

---

### 5.2.4 数据在主存中的存放

设存储字长64位（8字节），可读写4种数据长度：字节（8位）、半字（16位）、单字（32位）、双字（64位）。

#### 存放方法

| 方法 | 规则 | 优点 | 缺点 |
|---|---|---|---|
| **从起始位置存放** | 任何类型都从存储字起始位置开始 | 一周期完成读写 | 浪费存储空间 |
| **边界对齐存放** | 双字地址末3位=000，单字末2位=00，半字末位=0 | 一周期完成，减少浪费 | 仍有部分浪费 |
| **边界不对齐** | 数据可跨存储字存放 | 不浪费空间 | 可能需要两周期 |

> [!tip] 边界对齐核心
> 保证无论访问何种长度数据，都能在一个存取周期内完成。

---

## 5.3 半导体随机存储器和只读存储器

> [!note] 主存 = RAM（可读可写）+ ROM（只读）

### 5.3.1 RAM 记忆单元电路

**记忆单元**：存放一个二进制位的物理器件。地址相同的多个记忆单元构成一个存储单元。

| 类型           | 原理                 | 特点                      |
| ------------ | ------------------ | ----------------------- |
| **SRAM（6管）** | 双稳态触发器记忆信息（T₁~T₆管） | 速度快、集成度低、功耗大 → 用于高速/小容量 |
| **DRAM（4管）** | 利用MOS栅极电容存储电荷      | 集成度高、功耗小、速度慢 → 用于大容量主存  |
| **DRAM（单管）** | 一个MOS管 + 一个电容      | 功耗更小、集成度更高              |

---

### 5.3.2 动态RAM的刷新

> [!important] 为什么刷新？
> DRAM靠电容存储电荷，电容会漏电，需定时补充电荷以维持信息。

**刷新 vs 重写的区别：**

| | 刷新 | 重写（再生） |
|---|---|---|
| 触发 | 定时 | 破坏性读出后 |
| 范围 | 整行 | 单个存储单元 |

#### 三种刷新方式

| 方式       | 操作                            | 优点       | 缺点              |
| -------- | ----------------------------- | -------- | --------------- |
| **集中刷新** | 最大刷新间隔内集中安排刷新周期               | 读写不受刷新影响 | 有=="死区"==（停止读写） |
| **分散刷新** | 每个存取周期分两部分（读写+刷新）             | 无死区      | 加长存取周期，刷新过于频繁   |
| ==异步刷新== | 按平均间隔安排刷新周期（如2ms/32=62.5μs一次） | 死区最小，实用  | 需控制电路           |


**刷新注意事项：**
- 刷新对CPU**透明**
- 只需**行地址**，不需要列地址
- 不需输出数据，不需片选信号（所有芯片同时刷新）
- 应从**单个芯片**的存储容量着手考虑刷新

---

### 5.3.3 RAM芯片分析

#### 1. RAM芯片引脚

| 引脚    | 说明                        |
| ----- | ------------------------- |
| 地址线   | 单向输入，数目取决于容量（如1024×4→10根） |
| 数据线   | 双向，数目取决于位数                |
| 读写控制线 | 控制读/写操作                   |
| 片选线   | 决定芯片是否被选中                 |

#### DRAM地址复用

> [!important] 地址复用技术
> DRAM将地址分成**行地址**和**列地址**，分两次从相同引脚送入。
> - **RAS**（行地址选通信号）送入行地址
> - **CAS**（列地址选通信号）送入列地址
> - 每增加一条地址线 = 增加两位地址 = 增加**4倍**容量

#### 2. 地址译码方式

| 方式 | 结构 | 特点 | 适用 |
|---|---|---|---|
| **单译码（字选法）** | 字结构，M行 × b位矩阵，一条字线 | 简单但引线多 | 小容量 |
| ==双译码（重合法）== | X/Y两个方向译码交叉 | 减少引线，排列成方形矩阵 | 大容量 |

> 双译码方式可构成**位结构**（M×1）或**字段结构**芯片。

#### 3. RAM读写时序

- **SRAM**：需要地址有效 → CS有效 → 读写控制 → 数据交换
- **DRAM**：需要RAS/CAS分时控制，行/列地址分时输入

---

### 5.3.4 半导体只读存储器（ROM）

| 类型 | 特点 |
|---|---|
| **MROM（掩膜式）** | 制造时直接写入，无法更改。可靠性高、批量便宜 |
| **PROM（一次可编程）** | 用户可用编程器写入一次（熔丝烧断型/PN结击穿型） |
| **EPROM（可擦除可编程）** | 可多次改写，包括紫外线擦除（UVEPROM）和电擦除（EEPROM） |
| **闪速存储器（Flash）** | 不加电长期保存，在线快速擦除重写。NOR型（随机存取）和NAND型（区块读取） |

---

### 5.3.5 半导体存储器的封装

| 封装类型 | 特点 |
|---|---|
| **DIP（双列直插）** | 传统封装，容量有限（如64K×1） |
| **SIMM** | 30线（8位宽）或72线（32位宽） |
| **DIMM** | 168线（SDRAM）、184线（DDR）标准，数据线64位 |
| **RIMM** | 184线，Rambus专用，插槽必须全部插满或用终结器填充 |

---

## 5.4 主存储器的连接与控制

### 5.4.1 主存容量的扩展

$$
总芯片数 = \frac{总容量}{单片容量}
$$

#### 三种扩展方法

| 方法 | 方向 | 连接方式 | 举例 |
|---|---|---|---|
| **位扩展** | 加大字长 | 地址/片选/读写线并联，数据线独立 | 64K×1 → 64K×8（8片） |
| **字扩展** | 增加字数 | 数据/地址/读写线并联，片选区分 | 16K×8 → 64K×8（4片） |
| ==字位同时扩展== | 两者组合 | 上述两种组合 | 最常用 |

> [!example] 64K×8 存储器
> - 用64K×1芯片 → 位扩展需要8片
> - 用16K×8芯片 → 字扩展需要4片（高位地址A₁₅A₁₄译码产生片选）

---

### 5.4.2 存储芯片的地址分配和片选

片选由高位地址译码产生；片内字选由低位地址线直接连接。

| 方法 | 原理 | 优点 | 缺点 |
|---|---|---|---|
| **线选法** | 高位地址线直接接片选 | 简单，无需译码器 | 地址不连续，空间利用率低 |
| ==全译码法== | 全部高位地址线参与译码 | 地址唯一连续，便于扩展 | 译码电路复杂 |
| **部分译码** | 部分高位地址线译码 | 较简单 | ==地址重叠==（同一单元对应多个地址） |

---

### 5.4.3 主存储器和CPU的连接

#### 硬连接（三组总线）

| 总线 | 作用 |
|---|---|
| **地址总线（AB）** | CPU → 主存，传送地址 |
| **数据总线（DB）** | 双向，传送数据 |
| **控制总线（CB）** | 读写命令、MFC等 |

**接口寄存器：**
- **MAR**（存储器地址寄存器）：接收地址
- **MDR**（存储器数据寄存器）：缓冲数据

#### 软连接（读写操作）

**读操作流程：**
1. 地址 → MAR → AB
2. CPU 发 Read 命令
3. Wait for MFC（等待存储器完成）
4. M(MAR) → DB → MDR

**写操作流程：**
1. 地址 → MAR → AB
2. 数据 → MDR → DB
3. CPU 发 Write 命令
4. Wait for MFC

---

### 5.4.4 主存的校验

#### 1. 奇偶校验

- 微机中通常用**奇校验**：每字节 + 1位校验位，使"1"的个数为奇数
- 写入时计算校验位；读出时检查
- 只能==检错==，不能纠错

#### 2. ECC（错误检验与校正）

- 不仅能检错，还能**自动纠错**
- 用附加数据位存储"校验和"
- 读出时比较数据和ECC码，不匹配则校正后传给CPU
- 对**服务器**等关键任务至关重要

---

### 5.4.5 PC系列微机的存储器接口

| 接口位宽 | 特点 |
|---|---|
| **8位** | 4个时钟周期组成一个总线周期，读写8位 |
| **16位（8086）** | 分偶/奇两个存储体，BHE + A₀选择，支持规则字/字节访问 |
| **32位（80386/486）** | 4个存储体，4个BE引脚控制 |
| **64位（Pentium+）** | 8个存储体，8个BE信号控制 |

---

## 5.5 提高主存读写速度的技术

### 5.5.1 主存与CPU速度匹配

主存速度通常落后于CPU。以下技术不断演进以缩小差距。

### 各代DRAM技术对比

| 类型 | 突发模式 | 特点 |
|---|---|---|
| **FPM DRAM**（快速页模式） | 5-3-3-3 | 保持行地址不变，快速访问同行数据 |
| **EDO DRAM**（扩展数据输出） | 5-2-2-2 | 在FPM基础上减少等待，速度提升约22% |
| **SDRAM**（同步动态） | 5-1-1-1 | ==系统时钟同步==，双存储体交错访问 |
| **DDR SDRAM** | - | 上升沿+下降沿都传数据（==双沿传输==），184线 |
| **DDR2** | - | 4bit预取，4倍外部总线速度 |
| **DDR3** | - | 8bit预取，内核频率=接口频率的1/8 |
| **DDR4/DDR5** | - | 更高频率和带宽，DDR5预计可达6400MHz |
| **RDRAM（Rambus）** | - | ==窄通道==（16位），极高频率（400MHz→800MHz双沿），行/列地址总线分离 |

---

## 5.6 多体交叉存储技术

> [!note] 背景
> 主存速度成为瓶颈，除提高元件速度外，还可采用**多个存储器并行工作 + 交叉访问**。

### 5.6.1 并行访问存储器（单体多字系统）

- 多个并行工作的存储器共用一套地址寄存器和译码电路
- 同一地址并行访问各存储器的对应单元
- 假设每存储器字长w位，n个存储器同时访问 n×w 位

### 5.6.2 交叉访问存储器

> [!important] 核心思想
> 各存储模块有**各自独立**的地址寄存器、读写电路和数据寄存器。

**编址方式（以4体交叉为例）：**

低位地址译码选择存储体，高位地址选择体内存储字。

| 模块 | 编址序列 | 地址最低两位 |
|---|---|---|
| M₀ | 0, 4, 8, 12, ... | `00` |
| M₁ | 1, 5, 9, 13, ... | `01` |
| M₂ | 2, 6, 10, 14, ... | `10` |
| M₃ | 3, 7, 11, 15, ... | `11` |

> [!tip] 优点
> 连续访问时可以流水线式工作，各模块轮流响应，大幅提高等效带宽。

---

## 5.7 高速缓冲存储器（Cache）

> [!summary] 背景
> CPU速度年均提升60%，DRAM仅7%。SRAM组成的Cache速度接近/等于CPU速度。

### 5.7.1 高速缓存工作原理

#### 1. 程序的局部性原理

| 类型 | 含义 | 原因 |
|---|---|---|
| **时间局部性** | 访问过的单元可能很快再次被访问 | 程序循环 |
| **空间局部性** | 某单元被访问，邻近单元也可能被访问 | 指令顺序执行、数据聚集存储 |

利用局部性原理，将正在使用的部分放在==高速小容量Cache==中，使大多数访存针对Cache进行。

#### 2. Cache的基本结构

- Cache 和主存都被分成若干大小相等的**块**
- Cache 块数远少于主存块数
- 访问流程：用主存块号查Cache标记 → 比较是否命中
  - ==命中==：直接读Cache
  - ==不命中==：访问主存，调入对应块

---

### 5.7.2 Cache的读写操作

#### 读操作
- 命中 → 直接读Cache
- 不命中 → 访问主存，调入该块到Cache（若已满则替换）

#### 写操作

> [!important] 保持Cache与主存的一致性

| 策略 | 做法 | 特点 |
|---|---|---|
| **写直达法** | 同时写入Cache和主存 | 简单，保证一致性 |
| **写回法** | 只写Cache，被替换时才写回主存 | 减少主存写操作 |

**写不命中的处理：**
- **不按写分配法**：直接写主存，不调入Cache
- **按写分配法**：先调入Cache再写

---

### 5.7.3 地址映像

| 方式 | 原理 | 冲突概率 | 灵活性 | 实现难度 |
|---|---|---|---|---|
| **全相联映像** | 主存任意块可装入Cache任意位置 | ==最低== | 最灵活 | 最复杂 |
| **直接映像** | 主存每块只能放入Cache唯一指定位置 | ==最高== | 最不灵活 | 最简单 |
| ==组相联映像== | 组间直接映像 + 组内全相联（折衷方案） | 中等 | 中等 | 中等 |

---

### 5.7.4 替换算法

当Cache满时需要替换旧块：

| 算法 | 规则 | 特点 |
|---|---|---|
| **随机法（RAND）** | 随机选择 | 最简单 |
| **先进先出（FIFO）** | 替换最早调入的块 | 易实现，但可能换出常用块 |
| ==LRU（近期最少使用）== | 替换近期最少使用的块 | 最合理，但实现复杂 |

---

## 5.8 虚拟存储器

> [!summary] 核心思想
> 将主存和辅存统一编址，形成一个庞大存储空间。用户按虚地址编程，不必关心实际主存容量。

### 5.8.1 基本概念

- **虚地址（逻辑地址）**：用户编程地址
- **实地址（物理地址）**：实际主存地址
- 虚地址 >> 实地址

**工作流程：**
1. CPU 以虚地址访问
2. 硬件判断是否已在主存
3. 在 → 地址变换，直接访问
4. 不在 → 调入包含该字的一页/段，若主存满则替换
5. 由**操作系统 + 硬件**共同管理

---

### 5.8.2 页式虚拟存储器

- 主存和虚存都划分为大小相等的**页**（实页/虚页）
- 虚地址 = **虚页号** + **页内地址**
- 通过**页表**（虚页号→实页号对照表）实现地址变换

**优点：** 页长固定，管理方便
**缺点：** 最后一页有浪费；页不是逻辑独立实体，保护共享不便

---

### 5.8.3 段式虚拟存储器

- 按程序逻辑结构划分段（长度可变）
- 通过**段表**（含段号、装入位、段起点、段长）实现变换

**优点：** 逻辑独立，易于编译、管理、保护、共享
**缺点：** 段长不一，空间分配难，段间有浪费

---

### 5.8.4 段页式虚拟存储器

> [!important] 融合方案
> 程序按逻辑分**段**，每段再划分大小相等的**页**。虚存和实存以**页**为基本传送单位。

**地址结构：** 段号 + 段内页号 + 页内地址

**变换过程：**
段表起始地址 + 段号 → 段表 → 页表起始地址 + 段内页号 → 页表 → 实页号 + 页内地址 = 实地址

**优点：** 综合段式和页式的优点
**缺点：** 两级查表，速度较慢

---

### 快表与慢表

> [!tip] 加速地址变换
> - **慢表**：完整页表，存放于主存
> - ==快表（TLB）==：小容量高速存储器，存放当前最常用的页表信息
> - 先查快表，查不到再查慢表

---
