# 四天复习 Day 3：存储系统

> [!summary] 今日主线
> 今天只打一个硬仗：存储系统。  
> 核心问题是：**如何在速度、容量、成本之间折中，让 CPU 看起来有一个又快又大的存储器？**

覆盖章节：

- 第 5 章：存储系统和结构

---

## 0. 今日时间安排

| 时间块 | 任务 | 产出 |
|---|---|---|
| 50 min | 存储层次、分类、性能指标 | 会画存储层次图 |
| 80 min | 主存组织、芯片扩展、片选 | 会做容量扩展题 |
| 70 min | RAM/ROM、DRAM 刷新、校验 | 会区分 SRAM/DRAM/ROM |
| 90 min | Cache | 会算命中率、地址映像、替换 |
| 80 min | 虚拟存储器 | 会区分页式/段式/段页式、TLB |
| 40 min | 自测 + 公式回收 | 整理错题模板 |

---

## 1. 存储系统总主线

### 1.1 存储层次

```text
寄存器
Cache
主存
辅存
```

越靠上：

```text
速度越快
容量越小
成本越高
```

越靠下：

```text
速度越慢
容量越大
成本越低
```

### 1.2 为什么需要层次结构

CPU 速度远高于主存，主存容量又远小于辅存。存储层次利用程序局部性，让常用数据尽量靠近 CPU。

```text
时间局部性：刚访问过的内容很可能很快再访问
空间局部性：访问某地址后，很可能访问其附近地址
```

---

## 2. 主存组织和容量计算

### 2.1 基本指标

| 指标 | 含义 |
|---|---|
| 存储容量 | 可存储的信息总量 |
| 存取时间 | 从启动访问到完成访问的时间 |
| 存取周期 | 连续两次访问之间的最小时间间隔 |
| 存储带宽 | 单位时间传送的信息量 |

> [!warning] 易错
> 存取周期通常大于存取时间，因为存储器访问后还需要恢复。

### 2.2 容量单位

```text
1 B = 8 bit
1 KB = 2^10 B
1 MB = 2^20 B
1 GB = 2^30 B
```

### 2.3 地址线计算

```text
地址线 n 根 → 可寻址 2^n 个存储单元
```

若按字节编址：

```text
容量 = 2^n B
```

若按字编址：

```text
容量 = 2^n × 字长
```

---

## 3. RAM、ROM、DRAM 刷新

### 3.1 SRAM vs DRAM

| 对比 | SRAM | DRAM |
|---|---|---|
| 存储元 | 触发器 | 电容 |
| 是否刷新 | 不需要 | 需要 |
| 速度 | 快 | 慢 |
| 集成度 | 低 | 高 |
| 成本 | 高 | 低 |
| 常用位置 | Cache | 主存 |

### 3.2 DRAM 为什么刷新

DRAM 用电容存储电荷，电荷会泄漏，所以必须周期性刷新，否则数据丢失。

刷新方式常见：

```text
集中刷新
分散刷新
异步刷新
```

> [!warning] 易错
> 刷新是 DRAM 的要求，不是 SRAM 的要求。

### 3.3 ROM 分类

```text
MROM：掩膜 ROM，出厂写死
PROM：一次可编程
EPROM：紫外线擦除
EEPROM：电擦除
Flash：块擦除，常用于 U 盘、SSD
```

---

## 4. 主存容量扩展和片选

### 4.1 三种扩展

| 扩展方式 | 解决问题 | 做法 |
|---|---|---|
| 位扩展 | 字长不够 | 多片并联，地址线共用，数据线拼宽 |
| 字扩展 | 字数不够 | 多片分组，用高位地址片选 |
| 字位同时扩展 | 字数和字长都不够 | 先位扩展成组，再字扩展分组 |

### 4.2 题型模板

题目：用若干 `aK × b位` 芯片组成 `mK × n位` 存储器。

```text
位扩展片数 = n / b
字扩展组数 = m / a
总片数 = 位扩展片数 × 字扩展组数
```

片选：

```text
低位地址接芯片内部地址线
高位地址用于片选
```

> [!warning] 易错
> 位扩展不增加地址范围；字扩展才增加地址范围。

---

## 5. 主存校验

### 5.1 奇偶校验

```text
能发现奇数位错误
不能定位错误
不能可靠纠错
```

### 5.2 汉明码

纠正一位错：

```text
2^r ≥ k + r + 1
```

校验位放在：

```text
1, 2, 4, 8, ...
```

---

## 6. 多体交叉存储

### 6.1 为什么需要

单个主存体速度有限，多体交叉让多个存储体交替工作，提高连续访问带宽。

### 6.2 高位交叉 vs 低位交叉

| 方式 | 地址分布 | 特点 |
|---|---|---|
| 高位交叉 | 连续地址多在同一体 | 适合扩大容量 |
| 低位交叉 | 连续地址分布在不同体 | 适合提高连续访问速度 |

> [!warning] 易错
> 提高连续访问速度通常看低位交叉，不是高位交叉。

---

## 7. Cache

### 7.1 Cache 工作原理

Cache 利用局部性，把主存中近期可能访问的数据块复制到高速缓存中。

```text
CPU 访问 Cache
命中：直接取数
未命中：从主存调入块，再访问
```

### 7.2 命中率和平均访问时间

```text
平均访问时间 = 命中率 × Cache访问时间
             + 未命中率 × 未命中访问时间
```

如果题目给主存访问时间和 Cache 访问时间，要先看“未命中时是否包含 Cache 查询时间”，别机械套。

### 7.3 地址映像

| 映像方式 | 主存块能放哪里 | 优点 | 缺点 |
|---|---|---|---|
| 直接映像 | 只能放固定 Cache 行 | 简单快 | 冲突多 |
| 全相联映像 | 可放任意 Cache 行 | 冲突少 | 硬件复杂 |
| 组相联映像 | 可放指定组内任意行 | 折中 | 折中 |

### 7.4 地址字段

直接映像：

```text
主存地址 = 标记 Tag + Cache 行号 + 块内地址
```

全相联：

```text
主存地址 = 标记 Tag + 块内地址
```

组相联：

```text
主存地址 = 标记 Tag + 组号 + 块内地址
```

### 7.5 替换算法

```text
FIFO：先进先出
LRU：最近最少使用
Random：随机
```

### 7.6 写策略

| 策略 | 含义 |
|---|---|
| 写直达 | 同时写 Cache 和主存 |
| 写回 | 只写 Cache，替换时再写回主存 |

> [!warning] 易错
> Cache 和主存按块交换，不是按单个字节随便交换。

---

## 8. 虚拟存储器

### 8.1 基本思想

虚拟存储器让程序看到比实际主存更大的地址空间。主存和辅存之间按页/段调入调出。

```text
虚地址 → 地址变换 → 实地址
```

### 8.2 页式虚拟存储器

| 概念 | 含义 |
|---|---|
| 页 | 虚拟地址空间的固定大小块 |
| 页框 | 主存中的固定大小块 |
| 页表 | 记录虚页到实页框的映射 |
| 页内地址 | 页内偏移，不参与映射改变 |

地址结构：

```text
虚地址 = 虚页号 + 页内地址
实地址 = 实页号 + 页内地址
```

### 8.3 段式虚拟存储器

段按程序逻辑划分，长度可变。

```text
虚地址 = 段号 + 段内地址
```

### 8.4 段页式

```text
先分段，再分页
虚地址 = 段号 + 段内页号 + 页内地址
```

### 8.5 TLB

TLB = 快表，保存近期使用的页表项。

```text
TLB 命中：快速得到实页号
TLB 未命中：访问慢表页表
```

> [!warning] 易错
> Cache 解决 CPU 和主存速度差；虚拟存储器解决主存容量不够和地址空间管理问题。两者不是一回事。

---

## 9. 今日自测

1. 存储层次为什么能同时兼顾速度、容量和成本？
2. SRAM 和 DRAM 的区别是什么？
3. DRAM 为什么需要刷新？
4. 用 `16K × 4位` 芯片组成 `64K × 8位` 存储器，需要多少片？
5. 位扩展和字扩展的区别是什么？
6. 低位交叉为什么能提高连续访问速度？
7. Cache 三种地址映像方式怎么区分？
8. 写直达和写回有什么区别？
9. 页式虚拟存储器的虚地址和实地址怎么组成？
10. TLB 和 Cache 的作用分别是什么？

---

## 10. 今日过关标准

- 会做容量扩展题。
- 会写 Cache 三种地址格式。
- 能区分直接映像、全相联、组相联。
- 能区分 Cache、主存、虚拟存储器的作用。
- 能说清页式、段式、段页式的差别。

---

## 11. 晚上复盘

```text
今天公式最容易错的是：
1.
2.

今天概念最容易混的是：
1.
2.

明天开始前先看：
1.
2.
```
