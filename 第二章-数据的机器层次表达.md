# 第二章 数据的机器层次表示

> [!summary] 本章主线
> 同一串二进制位，在不同解释规则下含义不同。  
> 复习时按三层记：**考试结论 → 为什么 → 典型错法**。

## 0. 考前优先级

**优先保留**
- 原码 / 反码 / 补码转换
- 补码真值计算
- 定点整数和定点小数范围
- 浮点规格化
- IEEE 754 单精度转换
- BCD 和普通二进制的区别
- 奇偶校验、汉明码、CRC 的基本思想

**先降级了解**
- 汉字编码具体收录数量、Unicode 平面细节
- 临时浮点数 80 位格式
- CRC 生成多项式具体形式
- IA-32 各类数据类型细节

---

## 1. 无符号数

**考试结论**
- 所有位都是数值位，没有符号位。
- n 位范围：$0 \sim 2^n-1$。
- 8 位范围：0~255。

**为什么**
全 0 是最小值，全 1 是最大值：

```text
11111111 = 2^7 + 2^6 + ... + 2^0 = 255
```

**典型错法**
> [!warning] 易错
> `11111111` 按无符号数解释是 255，不是 -1。

---

## 2. 原码、反码、补码

### 2.1 机器数和真值

**考试结论**
- 机器数：符号也编码在二进制位中。
- 真值：数学意义上的正负数。

```text
机器数：10001011
真值：必须看编码规则，不能直接读
```

**为什么**
计算机只能存 0 和 1，正负号不能额外写在机器数前面。

**典型错法**
> [!warning] 易错
> 不要写 `-10000000`。机器数的符号已经在最高位里，`10000000` 是机器码，不是数学表达式。

### 2.2 三种码的规则

**考试结论**

| 规则 | 正数 | 负数 | 0 的表示 |
|---|---|---|---|
| 原码 | 符号位 0，数值为真值 | 符号位 1，数值为绝对值 | 两种 |
| 反码 | 与原码相同 | 符号位不变，数值位取反 | 两种 |
| 补码 | 与原码相同 | 反码末位 +1 | 唯一 |

例：8 位表示 $\pm 11$

```text
+11 原码/反码/补码：00001011
-11 原码：10001011
-11 反码：11110100
-11 补码：11110101
```

**为什么**
补码利用模运算把减法变成加法，符号位可以一起参加运算。

```text
4 位计数器模为 16
-2 等价于 +14
因为 8 - 2 和 8 + 14 在模 16 下结果相同
```

**典型错法**
> [!warning] 易错
> 反码负数只取反，不加 1；补码负数才取反 +1。补码负数不是“最高位为 1，后面直接当绝对值”。

### 2.3 三种码的范围

**考试结论**

| 对比点 | 原码 | 反码 | 补码 |
|---|---|---|---|
| 8 位整数 | -127~127 | -127~127 | -128~127 |
| n+1 位整数 | $-(2^n-1) \sim 2^n-1$ | $-(2^n-1) \sim 2^n-1$ | $-2^n \sim 2^n-1$ |
| n+1 位小数 | $-(1-2^{-n}) \sim 1-2^{-n}$ | $-(1-2^{-n}) \sim 1-2^{-n}$ | $-1 \sim 1-2^{-n}$ |

**为什么**
原码和反码都有 `+0`、`-0`，浪费一个编码；补码的 0 唯一，所以多出一个最小负数。

**典型错法**
> [!warning] 易错
> 8 位补码范围是 -128~127，不是 -127~127，也不是 -128~128。

### 2.4 补码反向解释：机器码如何变成真值

**考试结论**

正数最高位为 0：

```text
补码 = 原码 = 真值
00001011 = +11
```

负数方法 A：取反加一求绝对值

```text
11110101
取反：00001010
加一：00001011
所以真值 = -11
```

负数方法 B：权值法

```text
8 位补码权值：-128  64  32  16  8  4  2  1
11110101 = -128 + 64 + 32 + 16 + 4 + 1 = -11
```

**为什么**
补码整数最高位权值是负的。8 位时最高位是 $-2^7=-128$，所以 `10000000 = -128`。

**典型错法**
> [!warning] 易错
> 最高位为 1 时，取反加一得到的是绝对值，最后真值要加负号。权值法中最高位是负权，不是 +128。

### 2.5 高频易错：`10000000`

**考试结论**

```text
10000000 在无符号数中表示 128
10000000 在原码中表示 -0
10000000 在反码中表示 -127
10000000 在补码中表示 -128
```

**为什么**
二进制串本身没有固定含义，必须先确定解释规则。

**典型错法**
> [!danger] 高频易错
> 不要写 `-10000000`。题目说“机器数 10000000”时，它已经带着符号位了。

---

## 3. 定点数

### 3.1 定点整数

**考试结论**
小数点固定在最低位之后：

```text
X0X1X2...Xn.
```

| 表示方式 | 8 位范围 |
|---|---|
| 无符号整数 | 0~255 |
| 原码整数 | -127~127 |
| 反码整数 | -127~127 |
| 补码整数 | -128~127 |

**为什么**
最高位是否作为符号位，以及是否存在 `-0`，决定了可表示范围。

**典型错法**
> [!warning] 易错
> 定点整数的小数点是隐含的，不占存储位。

### 3.2 定点小数

**考试结论**
小数点固定在符号位之后：

```text
X0.X1X2...Xn
```

| 表示方式 | 范围 |
|---|---|
| 原码小数 | $-(1-2^{-n}) \sim 1-2^{-n}$ |
| 补码小数 | $-1 \sim 1-2^{-n}$ |

**为什么**
正数最大值是小数位全 1；补码小数最小值是 `1.0000 = -1`。

```text
0.1111 = 1/2 + 1/4 + 1/8 + 1/16 = 0.9375
```

**典型错法**
> [!warning] 易错
> 补码小数最大值到不了 1，只能到 $1-2^{-n}$。

### 3.3 补码小数权值法

**考试结论**

```text
X0.X1X2X3X4
-1  1/2  1/4  1/8  1/16
```

例：

```text
1.0000 = -1
1.1000 = -1 + 1/2 = -0.5
1.0100 = -1 + 1/4 = -0.75
0.1111 = 1/2 + 1/4 + 1/8 + 1/16 = 0.9375
```

**为什么**
补码小数和补码整数本质一样：

```text
整数最高位权值是 -2^n
小数最高位权值是 -1
```

**典型错法**
> [!warning] 易错
> `1.1000` 不是 -1.1000，也不是 -0.1000，而是 `-1 + 1/2 = -0.5`。

---

## 4. 浮点数

### 4.1 基本形式

**考试结论**

$$
N = M \times r^E
$$

| 部分 | 含义 |
|---|---|
| M | 尾数，表示有效数字 |
| r | 基数，通常是 2 |
| E | 阶码，表示小数点移动位置 |

**为什么**
浮点数用“尾数 + 阶码”扩大表示范围，代价是精度和运算复杂度。

**典型错法**
> [!warning] 易错
> 浮点数溢出主要看阶码是否越界。

### 4.2 尾数规格化判断

**考试结论**

| 表示方式 | 正数规格化 | 负数规格化 |
|---|---|---|
| 原码尾数 | `0.1xxx` | `1.1xxx` |
| 补码尾数 | `0.1xxx` | `1.0xxx` |

记忆句：

```text
原码看数值最高位是不是 1。
补码看符号位和最高数值位是否不同。
```

补码尾数规格化条件：

$$
m_s \oplus m_1 = 1
$$

**为什么**
规格化要求 $\frac{1}{2} \le |M| < 1$。补码负数中，`1.1xxx` 往往表示绝对值较小的负数，所以通常不是规格化负尾数。

**典型错法**
> [!warning] 易错
> 补码负尾数不要照搬原码规则：补码负数 `1.0xxx` 才是规格化。

### 4.3 移码

**考试结论**

$$
[X]_{移} = 偏置值 + X
$$

8 位整数常用偏置值 $2^7=128$。

| 特点 | 说明 |
|---|---|
| 最高位 | `0` 表负，`1` 表正 |
| 0 的表示 | 唯一：`10000000` |
| 与补码关系 | 最高位相反，其余位相同 |
| 比较大小 | 可按无符号数直接比较 |

**为什么**
阶码用移码，便于比较浮点数大小。

**典型错法**
> [!warning] 易错
> 移码最高位含义和补码相反。

### 4.4 IEEE 754 单精度

**考试结论**

```text
符号位 1 位 | 阶码 8 位 | 尾数 23 位
```

| 字段 | 含义 |
|---|---|
| S | 0 正，1 负 |
| E | 移码，偏置值 127 |
| M | 原码小数，规格化时隐含最高位 1 |

规格化数真值：

$$
(-1)^S \times 1.M \times 2^{E-127}
$$

**为什么**
规格化二进制数最高有效位一定是 1，IEEE 754 不存这个 1，相当于多保留一位精度。

**遇题怎么直接算**

十进制转单精度：

```text
1. 转二进制
2. 规格化成 1.xxx × 2^e
3. 阶码 = e + 127
4. 写 S、E、M
```

例：$(100.25)_{10}$

```text
100.25 = 1100100.01 = 1.10010001 × 2^6
S = 0
E = 127 + 6 = 133 = 10000101
M = 10010001000000000000000
结果：0 10000101 10010001000000000000000 = 42C88000H
```

单精度转十进制：

```text
1. 拆 S / E / M
2. 阶码真值 e = E - 127
3. 补上隐含 1，得到 1.M
4. 移动小数点
5. 看 S 确定正负
```

例：`C1C90000H`

```text
= 1 10000011 10010010000000000000000
S = 1
E = 131，所以 e = 4
M = 1.1001001
1.1001001 × 2^4 = 11001.001 = 25.125
结果：-25.125
```

**典型错法**
> [!warning] 易错
> 阶码字段不是阶码真值，单精度必须减 127。尾数前面有隐含的 `1.`，但这个 `1` 不写进 23 位尾数字段。

---

## 5. 非数值数据

### 5.1 ASCII

**考试结论**
- ASCII 是 7 位编码，共 128 个字符。
- 通常用 1 字节存一个 ASCII 字符。
- 数字、大小写字母在编码表中连续。

**为什么**
ASCII 解决英文字符、数字、控制符如何用二进制表示的问题。

**典型错法**
> [!warning] 易错
> 字符 `'1'` 和数值 `1` 不同。`'1'` 存的是 ASCII 码。

### 5.2 汉字编码四层关系

**考试结论**

| 编码 | 作用 |
|---|---|
| 区位码 | 用区号、位号定位汉字 |
| 国标码 | 用于交换 |
| 机内码 | 用于内部存储，避免和 ASCII 冲突 |
| 字形码 | 用点阵描述显示形状 |

常见转换：

$$
国标码 = 区位码 + 2020H
$$

$$
机内码 = 国标码 + 8080H
$$

**为什么**
汉字既要输入、交换、存储，又要显示，所以需要不同层次的编码。

**典型错法**
> [!warning] 易错
> 交换用国标码，内部存储用机内码，输出显示用字形码。

---

## 6. BCD

### 6.1 BCD 不是普通二进制

**考试结论**
BCD：用 4 位二进制表示 1 位十进制数字。

```text
十进制 59
普通二进制：00111011
BCD：0101 1001
```

**为什么**
BCD 不是为了更省空间，而是为了让计算机更方便地处理十进制数字。

**典型错法**
> [!warning] 易错
> `0101 1001` 按 BCD 是十进制 59；按普通二进制是 89。

### 6.2 为什么需要 BCD

**考试结论**
BCD 的意义：
- 方便显示：每 4 位直接对应一个十进制数字
- 方便人机输入输出
- 适合金额、计数、数字显示等十进制场景

BCD 的缺点：

```text
浪费空间。
4 位本来能表示 0~15，但 BCD 只用 0~9。
1010~1111 是非法码。
```

**为什么**
人习惯看十进制，机器习惯存二进制。BCD 保留了“每一位十进制数字”的边界。

**典型错法**
> [!warning] 易错
> BCD 不是进制转换后的二进制结果，而是“逐个十进制数字编码”。

### 6.3 常见 BCD 码

**考试结论**

| 码制 | 类型 | 特点 |
|---|---|---|
| 8421 码 | 有权码 | 最常见，非法码 1010~1111 |
| 2421 码 | 有权码 | 对 9 自补 |
| 余 3 码 | 无权码 | 8421 码 + 3，对 9 自补 |
| 格雷码 | 无权码 | 相邻码只有一位不同 |

常见 8421 码：

```text
0 0000    5 0101
1 0001    6 0110
2 0010    7 0111
3 0011    8 1000
4 0100    9 1001
```

**典型错法**
> [!warning] 易错
> 8421 BCD 中 `1010` 到 `1111` 非法，不表示十进制 10 到 15。

---

## 7. 校验码

### 7.1 奇偶校验

**考试结论**
- 码距为 2。
- 能发现一位错误。
- 不能可靠定位错误位置，也不能纠正错误。

| 校验方式 | 规则 |
|---|---|
| 奇校验 | 整个校验码中 `1` 的个数为奇数 |
| 偶校验 | 整个校验码中 `1` 的个数为偶数 |

**为什么**
一位出错会让 `1` 的个数奇偶性翻转，所以能检查出错误。

**典型错法**
> [!warning] 易错
> 奇偶校验能检错，不等于能纠错。

### 7.2 汉明码

**考试结论**
汉明码通过多个校验位定位错误。纠正 1 位错误时：

$$
2^r \ge k + r + 1
$$

校验位放在：

```text
1, 2, 4, 8, 16, ...
```

**为什么**
多个校验位形成“指误字”，指误字相当于错误位置编号。

**典型错法**
> [!warning] 易错
> 汉明码校验位不是随便放的，必须放在 2 的幂次位置。

### 7.3 CRC

**考试结论**
CRC 的核心是模 2 除法：

```text
发送端：信息位后补 0，除以生成多项式，余数作为校验位
接收端：收到的 CRC 码再除以生成多项式，余数为 0 表示通过
```

**为什么**
CRC 把二进制串看成多项式，通过余数建立“信息位 + 校验位”的关系。

**典型错法**
> [!warning] 易错
> CRC 的除法是模 2 除法，减法不借位，本质上就是异或。

---

## 8. 一页速记

**补码整数权值**

```text
8 位补码：-128  64  32  16  8  4  2  1
```

**补码小数权值**

```text
X0.X1X2X3X4
-1  1/2  1/4  1/8  1/16
```

**`10000000` 四种解释**

```text
无符号数：128
原码：-0
反码：-127
补码：-128
```

**规格化尾数**

| 表示方式 | 正数 | 负数 |
|---|---|---|
| 原码 | `0.1xxx` | `1.1xxx` |
| 补码 | `0.1xxx` | `1.0xxx` |

**BCD 记忆**

```text
BCD = 每 4 位表示 1 位十进制数字
59 的 BCD = 0101 1001
59 的普通二进制 = 00111011
```
