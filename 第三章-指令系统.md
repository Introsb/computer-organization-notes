# 第三章 指令系统

> [!summary] 本章主线
> 指令系统位于硬件和软件的交界面上。  
> 软件看到的是 `MOV`、`ADD`、`JMP`、`CALL`；硬件看到的是操作码、地址码、寻址方式、PC、SP、寄存器和主存访问。

本章核心问题：

1. 指令如何编码？
2. 操作码和地址码如何分配？
3. 操作数地址如何形成？
4. 堆栈如何支持零地址指令、`CALL`、`RET`？
5. CISC 和 RISC 体现了什么设计思想差异？

## 0. 复习分层

- `#必会`：考试主干，优先背熟。
- `#理解`：帮助你知道为什么这样设计。
- `#拓展`：保留知识完整性，先不抢主线。
- `#易错`：题目最喜欢绕的地方。
- `#例题`：看到题时直接套的模板。

---

## 1. 指令格式

### 1.1 机器指令基本格式 #必会

**考试主干**

```text
一条指令 = 操作码 + 地址码
```

| 字段 | 作用 |
|---|---|
| 操作码 | 说明“做什么”，如 ADD、SUB、MOV、JMP |
| 地址码 | 说明“操作数在哪、结果放哪、下一条去哪” |

指令长度取决于：

- 操作码字段长度
- 地址码个数
- 地址码长度

**理解解释**

一条机器指令不是自然语言，而是一串二进制编码。CPU 要从这串编码中识别：

1. 执行什么操作
2. 操作数在哪里
3. 结果放在哪里
4. 下一条指令在哪里

如果是顺序执行，下一条指令地址通常由 PC 自动形成，不必每条指令显式写出。

> [!info]- 拓展保留
> 指令长度与机器字长没有固定关系。
>
> | 指令字结构 | 特点 |
> |---|---|
> | 定长指令字结构 | 所有指令长度相同，便于译码 |
> | 变长指令字结构 | 不同指令长度不同，编码紧凑但译码复杂 |
>
> IBM 370、PDP-11 属于具体机器实例，可作为理解规整型/非规整型指令的例子。

**易错与题型**

> [!warning] 易错
> 指令长度不一定等于机器字长。机器字长是 CPU 一次能处理的数据宽度，指令长度是指令编码占多少位。

---

### 1.2 地址码结构 #必会

**考试主干**

双操作数指令理论上可能需要四类地址：

| 地址 | 含义 |
|---|---|
| A1 | 第一操作数地址 |
| A2 | 第二操作数地址 |
| A3 | 结果地址 |
| A4 | 下一条指令地址 |

常见地址结构：

| 类型 | 含义 |
|---|---|
| 四地址 | `(A1) OP (A2) → A3`，下一条地址为 A4 |
| 三地址 | `(A1) OP (A2) → A3`，下一条由 PC 给出 |
| 二地址 | `(A1) OP (A2) → A1`，结果覆盖 A1 |
| 一地址 | `(ACC) OP (A1) → ACC`，隐含累加器 ACC |
| 零地址 | 操作数来自堆栈，结果压回堆栈 |

**理解解释**

地址数减少，不是信息消失，而是信息被硬件约定隐藏。

```text
三地址：隐藏下一条指令地址
二地址：隐藏结果地址
一地址：隐藏一个操作数和结果地址
零地址：隐藏全部操作数地址，默认从堆栈取数
```

**易错与题型**

> [!warning] 易错
> 二地址指令的结果会覆盖一个源操作数。  
> 零地址指令不是没有操作数，而是操作数隐含在堆栈中。

| 对比 | 结论 |
|---|---|
| 三地址 | 程序短，但指令长 |
| 零地址 | 指令短，但程序可能长 |
| 二地址 | 大多数计算机常用 |

---

### 1.3 操作码与扩展操作码 #必会 #例题

**考试主干**

操作码说明指令要完成什么操作。若指令系统共有 m 条指令，规整型操作码至少需要 N 位：

$$
N \ge \lceil \log_2 m \rceil
$$

扩展操作码核心思想：

```text
地址数越多，操作码越短；
地址数越少，操作码越长。
```

因为地址字段减少后，空出来的位置可以借给操作码。

**理解解释**

扩展操作码本质上是一种“前缀编码”：

```text
前几位不是某个保留前缀 → 直接解释为三地址指令
前几位是保留前缀 → 继续向后看，后面的字段也是操作码的一部分
```

例如 16 位指令，基础操作码 4 位，地址字段各 4 位：

| 指令类型 | 编码特征 | 数量 |
|---|---|---|
| 三地址 | `0000~1110` | 15 条 |
| 二地址 | `1111 0000~1111 1110` | 15 条 |
| 一地址 | `1111 1111 0000~1111 1111 1110` | 15 条 |
| 零地址 | `1111 1111 1111 0000~1111 1111 1111 1111` | 16 条 |

**解题模板**

```text
当前层可用编码数 - 当前层所需指令数 = 可扩展前缀数

下一层可用编码数 = 可扩展前缀数 × 2^(下一个地址字段位数)

只要某一层：可用编码数 < 所需指令数
则无法实现。
```

> [!info]- 拓展保留：规整型与非规整型
> **规整型操作码**：操作码字段位数和位置固定，译码简单。
>
> IBM 370 的操作码字段一律为 8 位，常见格式有：
>
> | 格式 | 含义 |
> |---|---|
> | RR | 寄存器-寄存器 |
> | RX | 寄存器-变址 |
> | RS | 寄存器-存储 |
> | SI | 存储-立即数 |
> | SS | 存储-存储 |
>
> **非规整型操作码**：操作码字段位数不固定，也可能分散在不同位置，编码紧凑但译码复杂。
>
> PDP-11 字长 16 位，指令可分为单字长、两字长、三字长，操作码字段占 4~16 位不等。

**易错与题型**

> [!warning] 易错
> 扩展操作码不能把所有编码都用完。必须留下可扩展前缀，否则下一层地址数更少的指令无编码可用。

---

## 2. 寻址技术

### 2.1 编址、寻址与 EA #必会

**考试主干**

| 符号 | 含义 |
|---|---|
| A | 形式地址，指令地址码字段中给出的内容 |
| EA | 有效地址，真正访问主存操作数的地址 |
| S | 操作数，最终取到的数据 |

```text
形式地址 A --按寻址方式变换--> 有效地址 EA --访问主存/寄存器--> 操作数 S
```

**理解解释**

寻址方式的本质不是背名字，而是解决：如何用指令中有限的地址字段，找到真正的操作数。

> [!info]- 拓展保留：编址单位
> | 编址方式 | 特点 |
> |---|---|
> | 字编址 | 编址单位 = 访问单位，早期机器常用 |
> | 字节编址 | 编址单位与字节一致，适合非数值数据 |
> | 位编址 | 较少见 |
>
> 地址码位数与主存容量和最小寻址单位有关。主存容量越大，地址码越长；以字节为最小寻址单位，地址码通常更长。
>
> 例：主存容量 $2^{20}$ 字节，机器字长 32 位。  
> 按字节编址需要 20 位地址；按字编址需要 18 位地址。

**易错与题型**

> [!warning] 易错
> A 不一定就是 EA。只有直接寻址中 `EA = A`。

---

### 2.2 指令寻址和数据寻址 #必会

**考试主干**

| 类型 | 解决的问题 | 常见方式 |
|---|---|---|
| 指令寻址 | 下一条指令在哪里 | 顺序寻址、跳跃寻址 |
| 数据寻址 | 操作数在哪里 | 立即、直接、间接、寄存器、变址等 |

指令寻址：

```text
顺序寻址：PC 自动形成下一条指令地址
跳跃寻址：由 JMP/CALL/条件转移改变 PC
```

跳跃地址形成方式：

- 直接（绝对）寻址
- 相对寻址
- 间接寻址

**理解解释**

正常情况下 CPU 顺着 PC 一条条取指。遇到转移类指令时，PC 被改写，程序执行顺序发生变化。

**易错与题型**

> [!warning] 易错
> PC 指向下一条指令还是当前指令，要看具体教材约定和取指时序。做相对寻址题时，以题目给出的 PC 值为准。

---

### 2.3 基本寻址方式公式表 #必会 #例题

**考试主干**

| 寻址方式 | 公式 | 人话理解 |
|---|---|---|
| 立即寻址 | `S = A` | A 本身就是操作数 |
| 直接寻址 | `EA = A，S = M[A]` | A 就是真地址 |
| 间接寻址 | `EA = M[A]，S = M[M[A]]` | A 是地址的地址 |
| 寄存器寻址 | `S = R[i]` | 操作数在寄存器里 |
| 寄存器间接 | `EA = R[i]，S = M[R[i]]` | 寄存器里放地址 |
| 变址寻址 | `EA = A + R[x]` | 基准地址 + 下标 |
| 基址寻址 | `EA = R[b] + D` | 基址 + 位移 |
| 相对寻址 | `EA = PC + D` | 当前指令位置 + 位移 |
| 基址变址 | `EA = R[b] + R[x] + D` | 基址 + 变址 + 位移 |

**理解解释**

| 寻址方式 | 设计目的 |
|---|---|
| 立即寻址 | 速度快，适合常数 |
| 直接寻址 | 简单直观 |
| 间接寻址 | 扩大寻址范围，支持指针 |
| 寄存器寻址 | 速度快，地址字段短 |
| 寄存器间接 | 用寄存器保存主存地址 |
| 变址寻址 | 适合数组、表格、循环访问 |
| 基址寻址 | 适合程序重定位 |
| 相对寻址 | 适合位置无关代码和转移 |

> [!info]- 拓展保留：和 8086 对应
> 8086 中的 `[BX]`、`[SI]`、`[BX+SI]`、`[BP+SI+disp]`，本质上对应教材中的寄存器间接、变址、基址变址等方式。  
> 计组讲的是抽象原理，微机原理讲的是具体机器实现。

**易错与题型**

> [!warning] 易错
> 立即寻址的 A 是操作数，不是地址；直接寻址的 A 才是地址。

> [!warning] 易错
> 变址寻址不能只写 `R[x]`，必须加形式地址 A：`EA = A + R[x]`。

---

### 2.4 变型和组合寻址 #拓展

**考试主干**

| 类型 | 操作 | 记法 |
|---|---|---|
| 自增寻址 | `EA = R[i]`，然后 `R[i] ← R[i] + d` | `(R[i])+` |
| 自减寻址 | `R[i] ← R[i] - d`，然后 `EA = R[i]` | `-(R[i])` |

扩展变址：

| 方式 | 顺序 | 公式 |
|---|---|---|
| 前变址 | 先变址后间址 | `EA = M[A + R[x]]` |
| 后变址 | 先间址后变址 | `EA = M[A] + R[x]` |

页面寻址：

| 方式 | 有效地址 |
|---|---|
| 基页寻址 | `EA = 0 ∥ A` |
| 当前页寻址 | `EA = PC高位 ∥ A` |
| 页寄存器寻址 | `EA = 页寄存器 ∥ A` |

**理解解释**

组合寻址是把基本寻址方式叠起来使用，目的是用较短指令字段表示更灵活的地址。

**易错与题型**

> [!warning] 易错
> 前变址和后变址顺序不同，结果不同。看到题目要先判断是“先加后取内容”，还是“先取内容后加”。

---

## 3. 堆栈

### 3.1 堆栈基本概念 #必会

**考试主干**

```text
堆栈：后进先出 LIFO
SP：堆栈指针，始终指向栈顶
```

堆栈类型：

| 类型 | 特点 |
|---|---|
| 寄存器堆栈 | 速度快，容量小 |
| 存储器堆栈 | 容量大，栈顶浮动，需要 SP |

**理解解释**

堆栈不是靠移动所有数据实现“弹出”，而是靠修改 SP 改变栈顶位置。`POP` 后原存储单元里的内容可能还在，只是已经不属于当前有效栈内容。

> [!info]- 拓展保留：堆栈用途
> - 保存中断断点
> - 保存子程序返回地址
> - 保存状态标志和现场信息
> - 参数传递
> - 支持递归调用
> - 支持零地址指令计算

**易错与题型**

> [!warning] 易错
> SP 指向栈顶，但栈是向高地址增长还是向低地址增长，要看题目给的是“自底向上”还是“自顶向下”。

---

### 3.2 自底向上生成堆栈 #必会 #例题

**考试主干**

自底向上生成堆栈时，压栈先改 SP，再存数据：

```text
PUSH A：
SP ← SP - 1
M[SP] ← A

POP A：
A ← M[SP]
SP ← SP + 1
```

**理解解释**

这种堆栈向低地址方向增长，所以每压入一个数据，SP 要先减小，指向新的栈顶单元。

**易错与题型**

> [!warning] 易错
> PUSH 和 POP 修改 SP 的先后顺序不能乱。自底向上：PUSH 先减 SP，POP 后加 SP。

---

### 3.3 零地址指令和逆波兰表达式 #必会 #例题

**考试主干**

堆栈计算机中，算术逻辑类指令没有地址码字段：

1. 两个操作数从栈顶弹出
2. 送入运算器
3. 运算结果再压回栈顶

例：$a \times b + c \div d$

```text
逆波兰表达式：ab×cd/+

PUSH A
PUSH B
MUL
PUSH C
PUSH D
DIV
ADD
POP X
```

**理解解释**

零地址指令不是没有操作数，而是操作数的位置由堆栈规则隐含。后压入的数先参加运算，符合 LIFO。

> [!info]- 拓展保留：访存次数
> 执行一条零地址双操作数运算指令：
>
> | 堆栈类型 | 主存访问 |
> |---|---|
> | 软堆栈 | 通常需多次访存，教材常按 4 次分析 |
> | 硬堆栈 | 操作数在寄存器堆栈中，主存访问少 |

**易错与题型**

> [!warning] 易错
> 逆波兰表达式中，运算符出现时立即取栈顶两个数运算；减法、除法要注意两个操作数的先后顺序。

---

## 4. `JMP` / `CALL` / `RET`

### 4.1 程序控制指令 #必会

**考试主干**

| 指令 | 作用 |
|---|---|
| `JMP` | 无条件转移，不保存返回地址 |
| 条件转移 | 条件满足时改变 PC，否则顺序执行 |
| `CALL` | 调用子程序，必须保存返回地址 |
| `RET` | 根据返回地址回到主程序 |

**理解解释**

`CALL` 和 `JMP` 最大区别不是“都会跳转”，而是 `CALL` 要回来。因为要回来，所以必须保存返回地址。

子程序可以嵌套和递归，后调用的子程序先返回，正好符合堆栈 LIFO，所以返回地址最适合放在堆栈中。

> [!info]- 拓展保留：保存返回地址的方法
> | 方法 | 特点 |
> |---|---|
> | 子程序第一个字单元存放 | 简单，但不适合递归 |
> | 寄存器存放 | 速度快，但嵌套层数受限 |
> | 堆栈保存 | 最适合嵌套调用和递归 |

**易错与题型**

> [!warning] 易错
> `CALL` 会改变 PC，也会保存返回地址；`JMP` 只改变 PC，不负责回来。

---

## 5. 指令类型

### 5.1 数据传送类 #必会

**考试主干**

| 指令 | 作用 |
|---|---|
| `MOV` | 数据复制或移动 |
| `LOAD` | 主存到寄存器 |
| `STORE` | 寄存器到主存 |
| `PUSH` | 压栈 |
| `POP` | 出栈 |

**理解解释**

`MOV` 具有复制性质，一般不破坏源操作数。

**易错与题型**

> [!warning] 易错
> 数据传送不是算术运算，一般不改变源操作数本身。

---

### 5.2 运算类 #必会

**考试主干**

| 类型 | 例子 | 核心 |
|---|---|---|
| 算术运算 | ADD、SUB、MUL、DIV、INC、DEC、CMP | 数值计算 |
| 逻辑运算 | AND、OR、NOT、XOR | 位操作 |
| 移位运算 | 算术移位、逻辑移位、循环移位 | 位移动 |

逻辑运算常见用途：

| 操作 | 方法 |
|---|---|
| 位检查 | 和屏蔽字做 AND |
| 位清除 | 对应位为 0 的屏蔽字做 AND |
| 位置 1 | 对应位为 1 的屏蔽字做 OR |
| 位翻转 | 对应位为 1 的屏蔽字做 XOR |
| 判相等 | 两数 XOR，结果为 0 则相等 |

**理解解释**

算术和逻辑运算通常会影响状态标志位，如进位、溢出、零标志、符号标志等。

**易错与题型**

> [!warning] 易错
> 算术右移要保持符号位；逻辑右移通常补 0。带符号数和无符号数不能混用移位规则。

---

### 5.3 输入输出类 #拓展

**考试主干**

| 编址方式 | 特点 |
|---|---|
| 独立编址 | I/O 地址空间和主存地址空间分开，需要 `IN` / `OUT` |
| 统一编址 | 外设寄存器占用主存地址，可用普通访存指令访问 |

**理解解释**

独立编址把外设端口和主存地址分开，指令语义清楚；统一编址把外设寄存器当成主存单元访问，指令系统更统一。

> [!info]- 拓展保留：80x86 指令系统概览
> | 类别 | 指令举例 |
> |---|---|
> | 数据传送 | MOV、PUSH、POP |
> | 算术运算 | 加、减、比较、BCD 运算、ASCII 运算 |
> | 逻辑运算 | AND、OR、XOR、位测试 |
> | 移位 | 移位与循环指令 |
> | 程序控制 | JMP、CALL、RET |
> | 输入输出 | IN、OUT |

**易错与题型**

> [!warning] 易错
> 独立编址需要专门 I/O 指令；统一编址会占用一部分主存地址空间。

---

## 6. CISC / RISC / VLIW / EPIC

### 6.1 CISC 与 RISC #必会

**考试主干**

| 对比 | CISC | RISC |
|---|---|---|
| 指令数量 | 多 | 少 |
| 指令长度 | 可变 | 多为定长 |
| 指令格式 | 不规整 | 规整 |
| 寻址方式 | 多 | 少 |
| 单条指令功能 | 复杂 | 简单 |
| 硬件译码 | 复杂 | 较简单 |
| 流水线 | 较难 | 更容易 |
| 典型代表 | x86 | ARM、MIPS |

**理解解释**

CISC 的思想：让一条指令完成更复杂的功能，减少程序指令条数，但硬件译码和执行复杂。

RISC 的思想：让指令简单、规整、容易流水线化，用多条简单指令组合完成复杂功能。

> [!info]- 拓展保留：20%-80% 律
> 程序中常用的是少量简单指令：约 20% 的简单指令承担约 80% 的使用频度。  
> 这推动了 RISC 思想：重点优化高频简单指令。

**易错与题型**

> [!warning] 易错
> RISC 不是“功能弱”，而是用简单规整的指令组合实现复杂功能。

---

### 6.2 VLIW 和 EPIC #拓展

**考试主干**

| 概念 | 含义 |
|---|---|
| VLIW | 把多条可并行执行的简单指令打包成一条超长指令字 |
| EPIC | 显式并行指令代码，从 VLIW 思想发展而来 |

**理解解释**

VLIW / EPIC 的关键不是“指令更长”本身，而是把并行性更多交给编译器显式安排，让硬件执行单元能同时工作。

> [!info]- 拓展保留
> EPIC 强调由编译器显式暴露指令级并行性。Intel IA-64 是相关代表。

**易错与题型**

> [!warning] 易错
> VLIW 是把多条独立指令打包，不是让一条普通指令变得功能复杂。它和 CISC 的“复杂单指令”思路不同。

---

## 7. 一页速记

**指令格式**

```text
指令 = 操作码 + 地址码
操作码：做什么
地址码：操作数在哪、结果放哪、下一条去哪
```

**地址数减少的本质**

```text
三地址：隐藏下一条地址
二地址：隐藏结果地址
一地址：隐藏 ACC
零地址：隐藏堆栈
```

**扩展操作码**

```text
地址数越多，操作码越短；
地址数越少，操作码越长。
```

**寻址公式**

```text
立即：S = A
直接：EA = A
间接：EA = M[A]
寄存器：S = R[i]
寄存器间接：EA = R[i]
变址：EA = A + R[x]
基址：EA = R[b] + D
相对：EA = PC + D
基址变址：EA = R[b] + R[x] + D
```

**堆栈**

```text
LIFO
自底向上：
PUSH：SP ← SP - 1，M[SP] ← A
POP：A ← M[SP]，SP ← SP + 1
```

**CALL / JMP / RET**

```text
JMP：跳走，不保存返回地址
CALL：跳到子程序，保存返回地址
RET：按返回地址回来
```

**CISC / RISC**

```text
CISC：指令多、复杂、变长、寻址多，代表 x86
RISC：指令少、简单、规整、定长，代表 ARM/MIPS
```
