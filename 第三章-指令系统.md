# 第三章 指令系统

> [!summary] 章节概览
> 指令是指示计算机执行某些操作的命令，一台计算机的所有指令的集合构成该机的指令系统。指令系统是计算机的主要属性，位于硬件和软件的交界面上。本章讨论指令格式、寻址技术、堆栈操作、指令类型及指令系统的发展。

---

## 学习目标

| 层次 | 内容 |
|---|---|
| ==掌握== | 扩展操作码的方法、基本数据寻址方式和有效地址EA的计算、自底向上的存储器堆栈及进出栈操作 |
| ==理解== | 不同地址码（3/2/1/0地址）双操作数指令的区别、规整型/非规整型指令的特点、编址单位与地址码位数的关系、堆栈计算机中零地址指令的特点 |
| ==了解== | CISC与RISC的特点、VLIW和EPIC概念 |

---

## 3.1 指令格式

### 3.1.1 机器指令的基本格式

一条指令就是机器语言的一个语句，是一组有意义的二进制代码。

| 字段 | 含义 |
|---|---|
| **操作码** | 指明操作的性质及功能 |
| **地址码** | 指明操作数的地址 |

**指令长度**取决于操作码字段的长度、操作数地址的个数及长度。指令长度与机器字长没有固定关系。

| 类型 | 特点 |
|---|---|
| **定长指令字结构** | 所有指令长度相等 |
| **变长指令字结构** | 各种指令的长度随指令功能而异 |

---

### 3.1.2 地址码结构

对于一条双操作数指令，除操作码之外还应包含：

| 信息 | 表示 |
|---|---|
| 第一操作数地址 | A₁ |
| 第二操作数地址 | A₂ |
| 操作结果存放地址 | A₃ |
| 下条将要执行指令的地址 | A₄ |

这些信息可以**显式**给出（显地址），也可以依照约定**隐式**给出（隐地址）。

#### 五种地址结构

| 类型 | 格式 | 特点 |
|---|---|---|
| **四地址指令** | `(A₁) OP (A₂) → A₃, A₄=下条地址` | 直观但指令太长 |
| **三地址指令** | `(A₁) OP (A₂) → A₃, (PC)+1→PC` | 省去一个地址，仍需4次访存 |
| **二地址指令** | `(A₁) OP (A₂) → A₁, (PC)+1→PC` | 目的操作数被结果覆盖，需4次访存 |
| **一地址指令** | `(ACC) OP (A₁) → ACC, (PC)+1→PC` | 隐含累加器，仅需==2次访存== |
| **零地址指令** | 操作数隐含从堆栈弹出，结果压回堆栈 | 用于堆栈计算机 |

> [!tip] 地址数与程序长度的关系
> 对于同一个问题，用三地址指令编写的程序最短，但指令最长；用零地址指令编写程序最长，但指令最短。二地址指令为大多数计算机普遍采用。

---

### 3.1.3 指令的操作码

#### 1. 规整型操作码

操作码字段的位数和位置是**固定**的。

若指令系统共有 m 条指令，操作码字段位数为 N：

$$N \geq \log_2 m$$

> [!example] IBM 370 机（字长32位）
> 操作码字段一律为 8 位（可容纳 256 条指令），指令分为：
> - **RR型**（16位半字长）：寄存器-寄存器操作
> - **RX型**（32位单字长）：寄存器-变址操作
> - **RS型**（32位单字长）：寄存器-存储操作
> - **SI型**（32位单字长）：存储-立即数操作
> - **SS型**（48位一个半字长）：存储-存储操作

**IBM 370 指令字段含义：**

| 字母 | 含义 |
|---|---|
| R | 通用寄存器字段 |
| X | 变址寄存器字段 |
| B | 基址寄存器字段 |
| D | 位移量字段（12位） |
| I | 立即数字段（通常8位） |
| L | 长度字段 |

#### 2. 非规整型操作码

操作码字段的位数**不固定**，且分散地放在指令字的不同位置上。

> [!example] PDP-11 机（字长16位）
> 指令分为单字长、两字长、三字长三种，操作码字段占 4~16 位不等。

**PDP-11 指令字段含义：**

| 字母 | 含义 |
|---|---|
| S | 源操作数（Source） |
| D | 目的操作数（Destination） |
| R | 通用寄存器（R0~R7） |
| X | 索引/扩展 |

#### 3. ==扩展操作码法==

> [!important] 核心思想
> 让操作数地址个数多的指令操作码字段**短**，操作数地址个数少的指令操作码字段**长**。

**示例**（指令长度16位，操作码基础字段4位，三个4位地址码字段）：

| 指令类型 | 数量 | 操作码特征 |
|---|---|---|
| 三地址指令 | 15条 | `0000~1110` |
| 二地址指令 | 15条 | `1111 0000~1111 1110` |
| 一地址指令 | 15条 | `1111 1111 0000~1111 1111 1110` |
| 零地址指令 | 16条 | `1111 1111 1111 0000~1111 1111 1111 1111` |

> 通过扩展操作码法，原本最多16条指令的编码空间扩展到 ==61== 条。

---

## 3.2 寻址技术

> [!note] 基本概念
> - **编址**：对各种存储设备进行编码的方式
> - **寻址**：寻找操作数的地址或下一条将要执行的指令地址
> - 寻址技术包括**编址方式**和**寻址方式**

### 3.2.1 编址方式

#### 1. 编址单位

| 方式 | 特点 |
|---|---|
| **字编址** | 编址单位 = 访问单位，早期大多数机器采用 |
| **字节编址** | 编址单位与信息基本单位一致，适应非数值计算。访问单位通常是编址单位的若干倍 |
| **位编址** | 较少见 |

#### 2. 指令中地址码的位数

地址码位数与主存容量和最小寻址单位有关：

> 主存容量越大 → 地址码位数越长；以字节为最小寻址单位 → 地址码位数需要更长。

> [!example]
> 某机主存容量为 $2^{20}$ 字节，机器字长32位。按字节编址 → 地址码20位；按字编址 → 地址码只需18位。

---

### 3.2.2 指令寻址和数据寻址

| 类型 | 含义 | 实现方式 |
|---|---|---|
| **指令寻址** | 寻找下一条将要执行的指令地址 | 顺序寻址（PC+1）、跳跃寻址（转移指令） |
| **数据寻址** | 寻找操作数的地址 | 多种寻址方式 |

跳跃寻址的转移地址形成方式有三种：**直接（绝对）**、**相对**和**间接**寻址。

> 数据寻址的核心：指令中的形式地址 A —寻址方式→ 有效地址 EA

---

### 3.2.3 基本寻址方式

#### 1. 立即寻址

- 指令中直接给出操作数本身（立即数）
- 取指令时操作码和操作数同时取出，==不必再次访问主存==
- 缺点：操作数大小受指令长度限制，灵活性最差
- 用途：给寄存器或主存单元赋初值、提供常数

#### 2. 寄存器寻址

$$S = (R_i)$$

- 地址码给出通用寄存器的编号
- 优点：==存取速度快==、地址码字段短（寄存器数量少）
- 特点：缩短指令长度、提高指令执行速度

#### 3. 直接寻址

$$EA = A, \quad S = (A)$$

- 形式地址 = 有效地址，又称==绝对寻址==
- 优点：简单直观，便于硬件实现
- 缺点：地址空间受地址码字段位数限制

#### 4. 间接寻址

$$EA = (A), \quad S = ((A))$$

- 指令中给出的是存放操作数地址的主存单元地址（地址的地址）
- 通常指令中设一位标志位，间接寻址时 @=1

| 优点 | 缺点 |
|---|---|
| ==扩大寻址范围==（短地址访问大空间） | 降低了取操作数的速度 |
| 可将主存单元作为地址指针，修改指针即可改变操作数地址 | |

#### 5. 寄存器间接寻址

$$S = ((R_i))$$

- 寄存器中存放的是操作数的==有效地址==，操作数在主存中

#### 6. 变址寻址

$$EA = (R_x) + A, \quad S = ((R_x) + A)$$

- Rₓ 为变址寄存器，其内容称为变址值
- 适用于对数组等重复结构进行操作

#### 7. 基址寻址

$$EA = (R_b) + D, \quad S = ((R_b) + D)$$

- R_b 为基址寄存器，D 为位移量（可正可负）

#### 8. 相对寻址

$$EA = (PC) + D$$

- 是基址寻址的变通，基准地址由 ==PC== 提供
- 位移量 D 可正可负，操作数可能在指令地址之前或之后

> [!tip] 相对寻址的特点
> 1. 操作数地址随 PC 变化，与指令地址之间总相差一个固定值 +D
> 2. 程序可在主存中**任意浮动**（位置无关代码）

#### 9. 页面寻址

将主存空间分成大小相同的页，有效地址 = **页面地址** + **页内地址**。

| 方式 | 有效地址 | 特点 |
|---|---|---|
| **基页寻址** | EA = 0 ∥ A | 即直接寻址，操作数在零页面 |
| **当前页寻址** | EA = (PC)ₕ ∥ A | 操作数与指令在同一页面 |
| **页寄存器寻址** | EA = 页寄存器 ∥ A | 页面地址取自页寄存器 |

---

### 区分不同的寻址方式

| 方式 | 做法 |
|---|---|
| **显式** | 指令中设置专门的寻址方式字段，用二进制代码表明寻址方式类型 |
| **隐式** | 由操作码字段说明指令格式并隐含约定寻址方式 |

> [!warning] 注意
> 一条指令若有多个地址码，各地址码可采用**不同的寻址方式**。例如：`MOV AX, (BX)` 中源和目的可分别采用不同寻址方式。

---

### 3.2.4 变型或组合寻址方式

#### 1. 自增型寄存器间址和自减型寄存器间址

| 类型 | 操作 | 记法 |
|---|---|---|
| **自增寻址** | EA = (Rᵢ)，然后 Rᵢ ← (Rᵢ) + d | `(Rᵢ)+` |
| **自减寻址** | Rᵢ ← (Rᵢ) - d，然后 EA = (Rᵢ) | `-(Rᵢ)` |

其中 d 为修改量（按字节编址时 d=1，按字编址时 d=2）。

#### 2. 扩展变址方式（变址+间址组合）

| 方式 | 顺序 | 有效地址 |
|---|---|---|
| **前变址** | 先变址后间址 | EA = (A + (Rₓ))，S = ((A + (Rₓ))) |
| **后变址** | 先间址后变址 | EA = (A) + (Rₓ)，S = ((A) + (Rₓ)) |

#### 3. 基址变址寻址

$$EA = (R_b) + (R_x) + D$$

最灵活的一种寻址方式，基址和变址寄存器的内容均可改变。

---

## 3.3 堆栈与堆栈操作

> [!note] 堆栈定义
> 堆栈是一种按**"后进先出"（LIFO）**或**"先进后出"（FILO）**顺序进行存取的存储区。

### 3.3.1 堆栈结构

#### 1. 寄存器堆栈（硬堆栈）

- 用一组专门的寄存器构成
- 栈顶固定，各寄存器之间具有对应位**自动推移**功能
- 速度快但容量有限

#### 2. 存储器堆栈（软堆栈）

- 从主存中划出一段区域作堆栈
- 栈底固定，栈顶浮动
- 需要一个专门的硬件寄存器作为==堆栈指针 SP==，SP 始终指向栈顶单元

**自底向上生成堆栈的操作：**

| 操作 | 步骤 |
|---|---|
| **进栈（PUSH）** | ① (SP) - 1 → SP（修改栈指针）② (A) → (SP)（将数据压入堆栈） |
| **出栈（POP）** | ① ((SP)) → A（将栈顶内容弹出）② (SP) + 1 → SP（修改栈指针） |

---

### 3.3.2 堆栈操作

堆栈操作通过调整堆栈指针来改变栈顶位置，并不移动或抹除栈中已有内容。

**用途：** 保存中断断点、子程序返回地址、状态标志及现场信息、子程序参数传递。

#### 堆栈计算机中的零地址指令

在堆栈计算机中，堆栈是保存操作数和运算结果的**唯一场所**。算术逻辑类指令没有地址码字段（==零地址指令==）：

1. 两个操作数隐含地从堆栈顶部弹出
2. 送到运算器中进行运算
3. 运算结果再隐含地压入堆栈

> [!example] 逆波兰表达式求值
> 算术表达式 $a \times b + c \div d$ → 逆波兰表达式 $ab \times cd \div +$
>
> ```
> PUSH A    ; 数据a压入堆栈
> PUSH B    ; 数据b压入堆栈
> MUL       ; 完成 a×b
> PUSH C    ; 数据c压入堆栈
> PUSH D    ; 数据d压入堆栈
> DIV       ; 完成 c÷d
> ADD       ; 完成 a×b + c÷d
> POP X     ; 结果存入X单元
> ```

> [!warning] 访存次数
> 执行一条零地址的双操作数运算指令：若是**软堆栈**需访问 ==4次== 主存；若是**硬堆栈**只需访问 ==1次== 主存。

---

## 3.4 指令类型

指令按其功能可分为以下几种类型。

---

### 3.4.1 数据传送类指令

#### 1. 一般传送指令（MOV）

具有数据**复制**性质，源地址内容保持不变。

| 传送方向 | 助记符 |
|---|---|
| 主存单元之间 | MOV |
| 主存 → 寄存器 | LOAD |
| 寄存器 → 主存 | STORE |
| 寄存器之间 | MOV |

#### 2. 堆栈操作指令

| 指令 | 操作 |
|---|---|
| **PUSH** | 进栈（压入） |
| **POP** | 出栈（弹出） |

在程序中往往成对出现。

#### 3. 数据交换指令

双向传送，将源操作数与目的操作数相互交换位置。

---

### 3.4.2 运算类指令

#### 1. 算术运算指令

- 定点/浮点：加、减、乘、除
- 加1、减1、比较
- 部分机器有十进制算术运算指令
- 绝大多数会影响==状态标志位==（进位、溢出、全零、正负等）

#### 2. 逻辑运算指令

与、或、非、异或等，常用于数据字中某些位的操作：

| 操作 | 方法 | 用途 |
|---|---|---|
| **按位测（位检查）** | 屏蔽字对应位=1，其余=0，执行"与" | 检测特定位 |
| **按位清（位清除）** | 对应位=0，其余=1，执行"与" | 使某些位置0 |
| **按位置（位设置）** | 对应位=1，其余=0，执行"或" | 使某些位置1 |
| **按位修改** | 对应位=1，其余=0，执行"异或" | 翻转特定位 |
| **判符合** | 两数异或，结果为全0则两数相等 | 比较是否相等 |

#### 3. 移位类指令

| 类型 | 对象 | 特点 |
|---|---|---|
| **算术移位** | 带符号数 | 保持符号不变。左移1位 ≈ ×2，右移1位 ≈ ÷2 |
| **逻辑移位** | 无符号数 | 不考虑符号问题 |
| **循环移位** | — | 分小循环（不带进位）和大循环（带进位） |

---

### 3.4.3 程序控制类指令

#### 1. 转移指令

| 类型 | 特点 | 助记符 |
|---|---|---|
| **无条件转移** | 不受条件约束，直接转向新位置 | JMP |
| **条件转移** | 条件满足时才转移，否则顺序执行 | 各种条件分支 |

转移地址的形成：
- ==相对寻址==：转移地址 = (PC) + 位移量
- ==绝对寻址==：转移地址 = 指令地址码字段直接给出

#### 2. 子程序调用指令（CALL）

> 转子指令是一地址指令，安排在主程序中需要调用子程序的地方。

**转子指令 vs 转移指令的区别：**

| | 转移指令（JMP） | 转子指令（CALL） |
|---|---|---|
| 返回问题 | 不存在返回，无返回地址 | 必须保存返回地址 |
| 程序关系 | 同一程序内转移 | 不同程序之间调用 |

**保存返回地址的方法：**

| 方法 | 特点 |
|---|---|
| 子程序第一个字单元存放 | 可用于多重转子，==不能递归== |
| 寄存器存放 | 可实现递归循环 |
| ==堆栈保存== | 后进先出正好支持多重转子和递归循环 |

#### 3. 返回指令（RET）

- 子程序的最后一条指令一定是返回指令
- 返回地址通常保存在堆栈中，所以返回指令多是==零地址指令==
- 转子和返回指令也可以是带条件的

---

### 3.4.4 输入输出类指令

#### 1. 独立编址的 I/O

- 使用专门的 ==IN/OUT== 指令
- 端口地址与主存地址无关，是独立的地址空间
- 80x86 采用独立编址方式

#### 2. 统一编址的 I/O

- 外设寄存器和主存单元统一编址
- 不需要专门的 I/O 指令，用一般数据传送指令实现 I/O 操作
- 每个外设寄存器由唯一的主存地址来识别

**两种编址方式比较：**

| | 独立编址 | 统一编址 |
|---|---|---|
| **优点** | I/O指令与访存指令易区分；外设地址线少，译码简单；主存空间不减少 | 总线结构简单；全部访存类指令都可用于控制外设；可直接对外设寄存器进行各种运算 |
| **缺点** | 需增加 I/O Read和I/O Write控制信号 | 占用主存一部分地址，缩小可用主存空间 |

---

### 80x86 指令系统概览

| 类别 | 指令举例 |
|---|---|
| 数据传送 | MOV、PUSH/POP |
| 算术运算 | 加、减、比较、BCD运算、ASCII运算 |
| 逻辑运算 | 基本逻辑指令、位测试指令 |
| 移位 | 移位与循环指令 |
| 程序控制 | 转移指令、子程序调用和返回指令 |
| 输入输出 | IN/OUT 指令 |

---

## 3.5 指令系统的发展

### 3.5.1 x86 架构的扩展指令集

目前主流微机使用的指令系统都基于 x86 架构。Intel 和 AMD 各自开发了扩展指令集，包含处理器对多媒体、3D 处理等方面的支持。

### 3.5.2 从复杂指令系统到精简指令系统

#### "20%-80%律"

| 类型 | 占比 | 使用频度 |
|---|---|---|
| 简单指令 | 占指令总数 ==20%== | 使用频度 ==80%== |
| 复杂指令 | 占指令总数 ==80%== | 使用频度仅 ==20%== |

#### CISC vs RISC

| | CISC（复杂指令系统计算机） | RISC（精简指令系统计算机） |
|---|---|---|
| **设计思想** | 指令系统越复杂，计算机性能越强 | 简化指令系统，用简单指令组合实现复杂功能 |
| **指令特点** | 指令丰富、功能强、长度可变 | 指令精简、定长、格式规整 |
| **代表** | x86 系列 | ARM、MIPS |
| **提出者** | — | 1975年 IBM John Cocke |

### 3.5.3 VLIW 和 EPIC

| 概念 | 含义 | 特点 |
|---|---|---|
| **VLIW**（超长指令字） | 把许多条简单独立的指令组合到一条很长的指令字中 | 由编译器进行指令组合，被分解后分派到独立执行单元执行 |
| **EPIC**（显式并行指令代码） | 从 VLIW 衍生，将多条指令放入一个指令字 | 有效提高 CPU 各计算功能部件的利用效率 |

> 代表：Intel 的 IA-64 架构

---

