# 第九章 输入输出系统

> [!summary] 本章主线
> 输入输出系统的核心问题不是“外设有哪些”，而是：
> **主机如何通过接口与速度慢、类型多、工作方式各异的外设交换信息？**
>
> 本章主线可以压成一句话：  
> 接口负责“接上外设”，控制方式负责“谁来搬数据、什么时候搬、CPU 被打扰多少”。

本章核心问题：

1. 主机和外设之间为什么必须通过接口？
2. 接口、端口、端口地址分别是什么？
3. 程序查询、中断、DMA、通道四种 I/O 控制方式有什么区别？
4. CPU 响应中断需要什么条件？中断隐指令做什么？
5. DMA 为什么比中断更适合高速数据块传送？
6. 通道为什么可以看作 DMA 的进一步发展？

---

## 0. 一页总纲

### I/O 控制方式从低级到高级

```text
程序查询方式
CPU 一直问外设：准备好了吗？

程序中断方式
CPU 先去做别的事，外设准备好后主动叫 CPU。

DMA 方式
CPU 只做开始和结束管理，中间的数据块搬运交给 DMA 控制器。

通道方式
CPU 启动通道，通道执行通道程序，独立管理多个外设。
```

### 四种方式对比

| 方式 | 谁主要控制传送 | CPU 是否等待 | 适用场景 |
|---|---|---|---|
| 程序查询 | CPU 执行程序 | 是，循环查询 | 简单低速外设 |
| 程序中断 | CPU + 中断服务程序 | 否，外设准备好再打断 | 中低速外设、随机事件 |
| DMA | DMA 控制器 | 只在开始/结束参与 | 高速外设、成块数据 |
| 通道 | 通道处理器 | 只启动和结束时参与 | 大中型机、多外设并行 I/O |

> [!warning] 易错
> 这四种方式的本质区别，不是名字，而是 **CPU 参与数据传送的程度越来越低**。

---

## 9.1 主机与外设的连接

### 9.1.1 为什么需要 I/O 接口 #必会

**考试主干**

I/O 接口是主机和外设之间的交接界面。主机和外设不能直接裸连，主要因为：

- 速度不匹配：CPU 很快，外设通常很慢。
- 数据格式不匹配：主机常用并行二进制，外设可能串行、模拟或特殊编码。
- 电平和信号类型不匹配：主机内部信号与外设物理信号不同。
- 控制方式不一致：外设需要启动、停止、状态查询、握手等控制。
- 外设数量多：必须能识别和选择具体设备。

**理解解释**

接口像“翻译 + 缓冲 + 门牌号 + 协调员”。  
CPU 不需要知道打印机内部怎么走纸、磁盘怎么转，只需要通过接口发送命令、读写数据、检查状态。

---

### 9.1.2 主机与外设交换的信息 #必会

| 信息类型 | 方向 | 含义 | 例子 |
|---|---|---|---|
| 数据信息 | 双向 | 真正要交换的数据 | 键盘字符、磁盘块、打印内容 |
| 控制信息 | 主机 -> 外设 | 主机告诉外设做什么 | 启动、停止、设置参数 |
| 状态信息 | 外设 -> 主机 | 外设报告当前状态 | Ready、Busy、Error |
| 联络信息 | 双向 | 握手和同步 | REQ/ACK、Clock、Reset |
| 外设识别信息 | 外设 -> 主机 | 说明自己是谁、能做什么 | 设备 ID、设备类型、描述符 |

> [!tip] 记忆
> 数据是“传什么”，控制是“让你做什么”，状态是“你现在怎么样”，联络是“能不能开始说”，识别是“你是谁”。

---

### 9.1.3 接口的功能和组成 #必会

**接口功能**

| 功能 | 作用 |
|---|---|
| 通信联络控制 | 解决主机与外设时间配合问题 |
| 地址译码与设备选择 | 根据端口地址选中指定外设 |
| 数据缓冲 | 用缓冲寄存器弥补速度差 |
| 数据格式转换 | 并/串转换、串/并转换、A/D、D/A、编码转换 |
| 传递命令和状态 | 命令寄存器接收 CPU 控制，状态寄存器反馈外设状态 |

**接口基本组成**

| 部件 | 作用 |
|---|---|
| 数据端口 | 存放数据信息 |
| 命令端口 | 存放控制命令 |
| 状态端口 | 存放状态信息 |
| 地址译码电路 | 判断 CPU 访问的是不是本接口 |
| 控制逻辑 | 协调主机、接口、外设之间的操作 |

**接口和端口的区别**

```text
端口 = 接口中能被 CPU 直接访问的寄存器。
接口 = 若干端口 + 控制逻辑 + 译码/联络/转换电路。
```

> [!warning] 易错
> 端口不是外设本身。CPU 访问的是接口中的端口，再由接口去控制外设。

---

### 9.1.4 端口寻址 #必会

I/O 端口编址有两种方式：

| 编址方式 | 地址空间 | 访问方式 | 特点 |
|---|---|---|---|
| 独立编址 | I/O 端口地址空间与主存地址空间分开 | 专门 I/O 指令，如 `IN` / `OUT` | 不占主存地址，指令区分清楚 |
| 统一编址 | I/O 端口和主存单元统一编址 | 普通访存指令 | 指令系统简单，但占用主存地址空间 |

**理解解释**

独立编址把外设端口当作“另一套地址系统”；统一编址把外设端口当作“特殊的主存单元”。

> [!example] 80x86 独立编址
> 80x86 使用专门的 `IN` / `OUT` 指令访问 I/O 端口。  
> 直接寻址端口范围通常为 `0000H~00FFH`；间接寻址用 `DX` 给出 16 位端口地址，最多可寻址 64K 个端口。

---

### 9.1.5 接口类型 #了解

| 分类角度 | 类型 |
|---|---|
| 数据传送方式 | 并行接口、串行接口 |
| 主机控制方式 | 程序查询接口、中断接口、DMA 接口 |
| 功能灵活性 | 可编程接口、不可编程接口 |
| 通用性 | 通用接口、专用接口 |
| 信号类型 | 数字接口、模拟接口 |
| 应用对象 | 人机交互接口、外存接口、网络通信接口等 |

> [!tip] 复习重点
> 这一块不用逐项死背例子。重点是知道接口可以按“怎么传、谁控制、是否可编程、服务什么设备”分类。

---

## 9.2 程序查询方式及其接口

### 9.2.1 程序查询方式 #必会

**考试主干**

程序查询方式由 CPU 执行 I/O 程序实现数据传送。CPU 不断读取外设状态，直到外设准备好，才执行输入/输出操作。

```text
启动外设
    ↓
查询状态端口
    ↓
Ready = 0 ? 继续查询
    ↓
Ready = 1 ? 执行 I/O 数据传送
```

**理解解释**

程序查询方式最简单，但 CPU 会长期处于循环等待状态。外设慢时，CPU 很多时间都浪费在“问好了没有”。

**优缺点**

| 优点 | 缺点 |
|---|---|
| 硬件简单、成本低 | CPU 效率低 |
| 控制过程直观 | 主机和外设不能很好并行 |
| 适合简单系统 | 不适合高速或大量外设 |

> [!warning] 易错
> 程序查询不是“没有状态判断”。它的核心恰恰是 CPU 反复查询状态位。

---

### 9.2.2 查询式接口 #必会

程序查询方式接口至少需要：

| 寄存器/端口 | 作用 |
|---|---|
| 数据缓冲寄存器 / 数据端口 | 暂存 CPU 与外设传送的数据 |
| 状态寄存器 / 状态端口 | 保存 Ready、Busy 等状态标志 |

**输入接口**

```text
外设把数据送入数据缓冲寄存器
Ready 置 1
CPU 查询 Ready
CPU 读数据端口
Ready 清 0
```

**输出接口**

```text
CPU 查询 Busy
Busy = 0 时，CPU 写数据端口
接口锁存数据并送外设
Busy 置 1
外设取走数据后 Busy 清 0
```

> [!warning] 易错
> 输入常看 Ready，输出常看 Busy。  
> Ready 表示“数据已准备好可被 CPU 取走”；Busy 表示“外设/输出锁存器正忙，暂时别写”。

---

## 9.3 中断系统和程序中断方式

### 9.3.1 为什么需要中断 #必会

程序查询方式的问题：

1. CPU 长期踏步等待，效率低。
2. CPU 同一时间只能照看一台外设。
3. 不能及时处理随机异常事件。

程序中断方式的思想：

```text
CPU 启动外设后继续执行原程序
外设准备好后主动发中断请求
CPU 在允许中断时暂停原程序
转去执行中断服务程序
处理完后回到断点继续执行
```

**理解解释**

中断本质上是一次程序切换：从现行程序切到中断服务程序，再返回现行程序。为了能回来，必须保存断点和现场。

---

### 9.3.2 中断与子程序调用的区别 #易错

| 对比 | 程序中断 | 子程序调用 |
|---|---|---|
| 发生原因 | 外设请求、异常、故障等随机事件 | 程序中显式调用 |
| 发生时间 | 随机 | 由程序安排 |
| 处理对象 | 更紧急的外部/内部事件 | 程序功能分解 |
| 返回方式 | 中断返回指令 | 子程序返回指令 |
| 是否需要现场保护 | 通常需要保护现场 | 通常按调用约定保护 |

> [!warning] 易错
> 中断和 `CALL` 都会“跳过去再回来”，但中断不是程序员正常安排的函数调用，它由事件触发，带有随机性和优先级。

---

### 9.3.3 中断类型 #了解

| 分类 | 类型 | 含义 |
|---|---|---|
| 发生方式 | 自愿中断 | 程序中安排的中断，如软中断 |
| 发生方式 | 强迫中断 | 随机产生，由中断系统强制处理 |
| 处理方式 | 程序中断 | 执行中断服务程序 |
| 处理方式 | 简单中断 | 早期对 DMA 请求的一种叫法 |
| 来源 | 内中断 | CPU 内部硬件/软件原因，如溢出 |
| 来源 | 外中断 | CPU 外部部件引起，如 I/O 请求 |
| 屏蔽能力 | 可屏蔽中断 | 可由中断允许/屏蔽位控制 |
| 屏蔽能力 | 不可屏蔽中断 | 优先级高，常用于紧急故障 |
| 入口形成 | 向量中断 | 中断源提供向量地址 |
| 入口形成 | 非向量中断 | 需软件查询或统一入口再分派 |

---

### 9.3.4 中断源、中断请求和优先级 #必会

**中断源**

引起中断的事件或设备叫中断源。为了记录请求，通常给中断源配置中断请求触发器 `INTR`。

```text
中断源请求服务 → INTR 置 1 → CPU 检测到中断请求
```

**中断优先级**

优先级安排原则：越紧急、延迟后果越严重的中断，优先级越高。

常见顺序理解：

```text
故障中断 > DMA 请求/简单中断 > 一般 I/O 中断
```

**判优方法**

| 方法 | 特点 |
|---|---|
| 软件判优 | 用查询程序逐个检查请求，灵活但慢 |
| 硬件判优 | 用优先排队电路判定，速度快但硬件复杂，优先级固定 |

---

### 9.3.5 CPU 响应中断的条件 #必会

CPU 响应中断至少满足：

1. 有中断源发出中断请求。
2. CPU 接收到中断请求信号。
3. CPU 处于允许中断状态，即 `EINT = 1`。
4. 当前指令执行完毕，CPU 到达可响应中断的时刻。
5. 该请求未被屏蔽，并且通过优先级判定。

> [!warning] 易错
> CPU 不是随时中断正在执行的一条机器指令。通常是在当前指令执行完毕后才响应中断。

---

### 9.3.6 中断隐指令 #必会

CPU 响应中断后，由硬件自动完成一组操作，这些操作称为 **中断隐指令**。

中断隐指令不是真正的机器指令：

- 没有操作码。
- 不能由用户程序直接使用。
- 由硬件自动执行。

**中断隐指令主要完成**

| 操作 | 目的 |
|---|---|
| 保存断点 | 保存 PC，使中断结束后能返回原程序 |
| 关中断 | 防止保护现场过程中被新中断打断 |
| 识别中断源 / 形成入口地址 | 找到对应中断服务程序 |
| 转入中断服务程序 | 修改 PC，开始执行 ISR |

**向量地址两种情况**

| 情况 | 含义 |
|---|---|
| 向量地址就是入口地址 | CPU 直接转入中断服务程序 |
| 向量地址是向量表指针 | CPU 根据向量地址查中断向量表，再取 ISR 入口 |

> [!tip] 记忆
> 中断隐指令做的是“进门动作”：保存回来路、关门防打扰、找到服务程序入口、跳过去。

---

### 9.3.7 中断处理全过程 #必会

中断全过程：

```text
中断请求
→ 中断判优
→ 中断响应
→ 中断处理
→ 中断返回
```

中断服务程序通常分三部分：

| 部分 | 主要任务 |
|---|---|
| 准备部分 | 保护现场；非向量中断还要确定中断源；必要时开中断以允许更高级中断嵌套 |
| 处理部分 | 真正执行为中断源服务的程序 |
| 结尾部分 | 关中断；恢复现场；开中断；执行中断返回指令 |

**中断嵌套**

中断嵌套要求：

- 能保存多个断点和现场。
- 后发生且优先级更高的中断可打断低级中断服务程序。
- 断点和现场通常用堆栈保存，符合后保护先恢复。

> [!warning] 易错
> “中断隐指令”主要是硬件自动完成的响应阶段；“中断服务程序”是软件执行的处理阶段。两者不要混。

---

### 9.3.8 程序中断接口结构 #了解

具有中断能力的外设接口 = 查询式接口 + 中断控制机构。

中断控制机构包括：

- 中断请求电路
- 中断优先级电路
- 向量地址形成部件

---

## 9.4 DMA 方式及其接口

### 9.4.1 DMA 基本思想 #必会

DMA = Direct Memory Access，直接存储器访问。

**考试主干**

DMA 在外设和主存之间开辟直接数据通道，不需要 CPU 逐字搬运数据。传送过程由 DMA 控制器控制。

```text
外设 ⇄ DMA 控制器 ⇄ 主存
```

CPU 的主要工作：

```text
传送前：初始化 DMA 控制器
传送中：让出总线，由 DMA 控制器搬数据
传送后：响应 DMA 结束中断，做后处理
```

**DMA 特点**

- 主存既可被 CPU 访问，也可被外设通过 DMA 访问。
- 主存地址和传送长度由 DMA 硬件自动修改。
- 传送速度快，适合高速外设和数据块传送。
- 开始前需要 CPU 预处理，结束后通常用中断做后处理。

---

### 9.4.2 DMA 和中断的区别 #必会 #易错

| 对比 | 程序中断方式 | DMA 方式 |
|---|---|---|
| 本质 | 程序切换 | 硬件直接搬运数据 |
| CPU 参与 | 执行中断服务程序完成传送 | 只在开始和结束参与 |
| 是否保护现场 | 需要保护和恢复现场 | 传送中通常不破坏 CPU 现场 |
| 响应时刻 | 通常在一条指令结束后 | 可在一个机器周期结束后 |
| 传送速度 | 较低 | 高 |
| 适用 | 中低速外设、随机事件 | 高速外设、成块数据 |
| 能否处理异常事件 | 可以 | 主要限于数据块 I/O |
| 优先级 | 通常低于 DMA 请求 | DMA 请求优先级较高 |

> [!warning] 易错
> DMA 结束时仍可能用中断通知 CPU，但数据搬运过程不是由中断服务程序逐字完成的。

---

### 9.4.3 DMA 控制器 #必会

**DMA 控制器功能**

1. 接受外设 DMA 请求，并向 CPU 发总线请求。
2. 接收 CPU 总线响应后，接管系统总线。
3. 给出主存地址和传送长度。
4. 自动修改地址计数器和长度计数器。
5. 控制数据传送方向。
6. 传送结束后向 CPU 报告。

**DMA 控制器基本组成**

| 部件 | 作用 |
|---|---|
| 主存地址计数器 | 保存当前传送的主存地址，每传一个数据自动修改 |
| 传送长度计数器 | 保存剩余传送长度，计到 0 表示结束 |
| 数据缓冲寄存器 | 暂存每次传送的数据 |
| DMA 请求触发器 | 记录外设 DMA 请求 |
| 控制/状态逻辑 | 协调方向、时序、状态和请求响应 |
| 中断机构 | 数据块传送结束后向 CPU 发中断请求 |

**关键联络信号**

| 信号 | 方向 | 含义 |
|---|---|---|
| `DREQ` | 外设 -> DMA 控制器 | DMA 请求 |
| `DACK` | DMA 控制器 -> 外设 | DMA 响应 |
| `HRQ` | DMA 控制器 -> CPU | 总线请求 |
| `HLDA` | CPU -> DMA 控制器 | 总线响应/总线授权 |

---

### 9.4.4 DMA 控制器连接和传送 #必会

以外设向主存写入数据块为例：

```text
1. 外设向 DMA 控制器发 DREQ
2. DMA 控制器向 CPU 发 HRQ
3. CPU 当前机器周期结束后发 HLDA，让出总线
4. DMA 控制器向外设发 DACK
5. DMA 控制器给出主存地址
6. 外设数据经 DMA 控制器/数据总线写入主存
7. 地址计数器 +1，长度计数器 -1
8. 重复传送直到长度计数器为 0
9. DMA 控制器撤销 HRQ，把总线交还 CPU
10. DMA 控制器向 CPU 发中断，请求后处理
```

> [!tip] 关键线索
> `DREQ/DACK` 是 DMA 控制器和外设之间的握手；`HRQ/HLDA` 是 DMA 控制器和 CPU 之间的总线控制权交接。

---

### 9.4.5 DMA 传送方法 #必会

| 方法 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| CPU 停止访问主存法 | DMA 获得总线后连续传送整个数据块 | DMA 传送效率高 | CPU 暂停访问主存，影响 CPU 工作 |
| 存储器分时法 | 一个存取周期分成 CPU 时间片和 DMA 时间片 | CPU 和 DMA 交替访问，几乎无总线交接开销 | 要求主存速度更高 |
| 周期挪用法 | DMA 每次只挪用一个存取周期传一个字/字节 | 对 CPU 影响较小，折中常用 | DMA 传送不是连续独占 |

**理解解释**

```text
停止访问主存法：DMA 一口气搬完，CPU 等。
存储器分时法：CPU 和 DMA 固定分时用主存。
周期挪用法：DMA 需要时偷一个周期，用完还给 CPU。
```

> [!warning] 易错
> 周期挪用不是让 CPU 完全停止，而是临时让出一个或少数几个主存周期。

---

### 9.4.6 DMA 传送过程 #必会

DMA 传送可分为三阶段：

| 阶段 | 谁完成 | 内容 |
|---|---|---|
| DMA 预处理 | CPU | 测试外设状态，设置 DMA 控制器寄存器，指定地址、长度、方向，启动外设 |
| 数据传送 | DMA 控制器 | 接管总线，按硬件流程传送数据，自动修改地址和计数 |
| DMA 后处理 | CPU | DMA 结束后响应中断，检查状态，处理结束事务 |

> [!warning] 易错
> DMA 流程图中的“循环传送”不是 CPU 执行程序循环，而是 DMA 控制器硬件执行。

---

## 9.5 通道控制方式

### 9.5.1 通道基本概念 #了解

通道控制方式是 DMA 方式的进一步发展。通道是具有特殊功能的处理器，可以执行通道程序，统一管理外设和主存之间的数据传送。

**通道与 DMA 的区别**

| 对比 | DMA 控制器 | 通道 |
|---|---|---|
| 控制方式 | 专门硬件控制逻辑 | 执行通道程序 |
| 独立性 | 较低 | 更强 |
| 管理设备 | 一台或少数同类设备 | 多台同类或不同类设备 |
| 功能复杂度 | 主要完成数据块传送 | 能执行通道指令，管理 I/O 过程 |

> [!tip] 记忆
> DMA 是“硬件搬运工”；通道是“会执行 I/O 程序的专用处理器”。

---

### 9.5.2 通道类型 #了解

| 类型 | 适用设备 | 工作特点 |
|---|---|---|
| 字节多路通道 | 多台低速设备 | 多个设备分时交叉传送，一个设备一次传一个字节 |
| 选择通道 | 高速设备 | 一段时间只选择一台设备，该设备独占通道连续传送 |
| 数组多路通道 | 多台高速设备 | 兼有字节多路和选择通道特点，可分时共享，也可成组传送 |

**理解解释**

```text
字节多路通道：轮流照顾很多慢设备。
选择通道：一次专心服务一个快设备。
数组多路通道：设备传数据时专心服务，设备做辅助操作时切去服务别的设备。
```

---

### 9.5.3 通道结构和工作过程 #了解

**通道结构常见寄存器**

| 寄存器 | 作用 |
|---|---|
| CCWR | 通道命令字寄存器，存放通道命令字 CCW |
| CAWR | 通道地址字寄存器，指出 CCW 在主存中的地址 |
| CSWR | 通道状态字寄存器，记录通道和设备状态 |

**通道工作过程**

```text
1. 用户程序通过访管指令进入管理程序
2. CPU 由管理程序组织通道程序并启动通道
3. 通道执行通道程序，完成 I/O 工作
4. 通道程序结束后向 CPU 发中断请求
5. CPU 响应中断，调用管理程序处理结束状态
```

**核心理解**

每完成一次 I/O，CPU 主要被打扰两次：

```text
启动通道时一次
通道结束中断时一次
```

---

## 9.6 四种 I/O 控制方式总对比 #必会

| 对比项 | 程序查询 | 程序中断 | DMA | 通道 |
|---|---|---|---|---|
| CPU 参与程度 | 最高 | 较高 | 较低 | 最低 |
| 数据传送执行者 | CPU 程序 | CPU 中断服务程序 | DMA 控制器 | 通道 |
| CPU 是否等待外设 | 等待 | 不等待 | 基本不等待 | 基本不等待 |
| 外设主动性 | 低 | 可发中断请求 | 可发 DMA 请求 | 通道统一管理 |
| 硬件复杂度 | 最低 | 中等 | 较高 | 最高 |
| 适用速度 | 低速 | 中低速 | 高速块传送 | 多设备、高并发 I/O |

### 从 CPU 被打扰程度理解

```text
程序查询：CPU 一直被占用
程序中断：外设准备好时打扰 CPU
DMA：开始和结束打扰 CPU，中间不打扰
通道：CPU 只启动通道和处理结束中断
```

### 从“谁搬数据”理解

```text
查询/中断：CPU 搬数据
DMA：DMA 控制器搬数据
通道：通道按通道程序组织 I/O
```

---

## 9.7 高频易错

### 1. 接口 vs 端口

```text
端口：CPU 能直接访问的寄存器
接口：端口 + 控制逻辑 + 地址译码 + 联络/转换电路
```

### 2. 状态信息 vs 控制信息

```text
状态信息：外设告诉 CPU “我怎么样”
控制信息：CPU 告诉外设 “你做什么”
```

### 3. 程序查询 vs 程序中断

```text
查询：CPU 主动反复问，浪费 CPU
中断：外设准备好后主动叫 CPU
```

### 4. 中断隐指令 vs 中断服务程序

```text
中断隐指令：硬件自动完成，保存断点、关中断、形成入口
中断服务程序：软件执行，真正处理设备/事件
```

### 5. DMA vs 中断

```text
中断：程序切换，由 CPU 执行服务程序
DMA：硬件搬运数据块，CPU 只做预处理和后处理
```

### 6. DMA 三种传送方法

```text
CPU停止访问主存：DMA 连续占用，CPU 等
存储器分时：CPU 和 DMA 固定分时访问主存
周期挪用：DMA 临时借一个周期传一个数据
```

### 7. 通道 vs DMA

```text
DMA 控制器：专用硬件逻辑，适合数据块搬运
通道：能执行通道程序的专用处理器，能管理多个外设
```

---

## 9.8 一页速记

**I/O 接口**

```text
接口 = 主机和外设的交接界面
端口 = 接口中 CPU 可访问的寄存器
```

**三类核心端口**

```text
数据端口：传数据
命令端口：发控制命令
状态端口：读外设状态
```

**端口编址**

```text
独立编址：I/O 地址和主存地址分开，用 IN/OUT
统一编址：I/O 端口当作主存单元访问
```

**中断条件**

```text
有请求
CPU 接收到请求
EINT = 1
未屏蔽
优先级通过
当前指令执行完毕
```

**中断隐指令**

```text
保存断点
关中断
识别中断源 / 形成入口地址
转入中断服务程序
```

**DMA 流程**

```text
CPU 预处理
DMA 控制器传送数据
DMA 结束后中断 CPU 做后处理
```

**通道**

```text
通道 = 能执行通道程序的 I/O 专用处理器
CPU 只负责启动和结束处理
```
