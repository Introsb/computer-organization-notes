# 四天复习 Day 1：数据表示与机器运算

> [!summary] 今日主线
> 今天解决“计算机如何表示数据、如何做运算”。  
> 重点不是背一堆码制，而是把 **二进制解释规则 → 定点/浮点表示 → 补码运算 → 浮点运算** 串起来。

覆盖章节：

- 第 1 章：概述中的冯·诺依曼、五大部件、性能指标
- 第 2 章：数据的机器层次表示
- 第 4 章：数值的机器运算

---

## 0. 今日时间安排

| 时间块 | 任务 | 产出 |
|---|---|---|
| 45 min | 第 1 章快速过主线 | 会画五大部件和总线连接 |
| 90 min | 第 2 章数据表示 | 会算补码真值、定点范围、IEEE754 |
| 90 min | 第 4 章定点运算 | 会判溢出、做补码加减、理解移位 |
| 70 min | 第 4 章乘除和浮点运算 | 会说清原码/补码乘除和浮点加减流程 |
| 40 min | 自测 + 错题回收 | 标出 5 个最不稳知识点 |

---

## 1. 第 1 章只抓骨架

### 必会主干

```text
冯·诺依曼计算机 = 存储程序 + 程序控制
五大部件 = 运算器、控制器、存储器、输入设备、输出设备
CPU = 运算器 + 控制器
主机 = CPU + 主存
```

### 必背解释

程序和数据都以二进制形式存放在主存中，CPU 按地址取指令，指令驱动数据流动。

### 高频计算

```text
存储容量 = 存储单元个数 × 每单元位数
CPU 执行时间 = 指令条数 × CPI × 时钟周期
MIPS = 指令条数 / (执行时间 × 10^6)
```

### 自测

- 冯·诺依曼结构的基本特点是什么？
- CPU、主机、外设分别是什么？
- 机器字长、数据通路宽度、存储容量有什么区别？

---

## 2. 第 2 章：数据表示

### 2.1 今日最重要的判断句

```text
同一串二进制位，在不同解释规则下含义不同。
```

例：

```text
10000000
无符号数：128
原码：-0
反码：-127
补码：-128
```

> [!warning] 易错
> 不要写 `-10000000`。机器数的符号已经在最高位里。

### 2.2 补码必须会反着读

方法 A：取反加一求绝对值

```text
11110101
取反：00001010
加一：00001011
真值 = -11
```

方法 B：权值法

```text
8 位补码权值：
-128  64  32  16  8  4  2  1

11110101 = -128 + 64 + 32 + 16 + 4 + 1 = -11
```

### 2.3 定点范围

| 类型 | 8 位范围 |
|---|---|
| 无符号整数 | 0~255 |
| 原码整数 | -127~127 |
| 反码整数 | -127~127 |
| 补码整数 | -128~127 |

补码小数：

```text
X0.X1X2X3X4
-1  1/2  1/4  1/8  1/16

1.1000 = -1 + 1/2 = -0.5
```

### 2.4 浮点规格化

| 表示方式 | 正数规格化 | 负数规格化 |
|---|---|---|
| 原码尾数 | `0.1xxx` | `1.1xxx` |
| 补码尾数 | `0.1xxx` | `1.0xxx` |

补码尾数判断：

```text
符号位和最高数值位不同
m_s ⊕ m_1 = 1
```

### 2.5 IEEE754 单精度

```text
S 1 位 | E 8 位 | M 23 位
真值 = (-1)^S × 1.M × 2^(E-127)
```

十进制转单精度：

```text
1. 转二进制
2. 规格化成 1.xxx × 2^e
3. 阶码字段 E = e + 127
4. 写 S、E、M
```

---

## 3. 第 4 章：机器运算

### 3.1 加法器和进位

必须理解：

```text
串行进位：硬件简单，速度慢
并行进位：硬件复杂，速度快
```

进位逻辑：

```text
G_i = A_i B_i       产生进位
P_i = A_i ⊕ B_i     传递进位
C_{i+1} = G_i + P_i C_i
```

### 3.2 补码加减

核心：

```text
[X + Y]_补 = [X]_补 + [Y]_补
[X - Y]_补 = [X]_补 + [-Y]_补
```

符号位参加运算，最高位进位丢弃。

### 3.3 溢出判断

| 方法 | 判断 |
|---|---|
| 双符号位 | `00` 正，`11` 负，`01` 正溢，`10` 负溢 |
| 单符号位 | 同号相加，结果变号则溢出 |
| 进位法 | 符号位进位和最高数值位进位不同则溢出 |

> [!warning] 易错
> 无符号数看进位；补码带符号数看溢出。进位和溢出不是一回事。

### 3.4 移位

```text
逻辑移位：空位补 0，适合无符号数
算术移位：保持符号，适合带符号数
循环移位：移出的位补回另一端
```

### 3.5 乘除法只抓流程

原码乘法：

```text
符号单独处理
数值部分按无符号乘法做
```

补码 Booth 乘法：

```text
看相邻两位：
01 加被乘数
10 减被乘数
00 / 11 不操作
然后算术右移
```

除法：

```text
恢复余数法：不够减就恢复
加减交替法：不恢复，下一步用加法修正
```

### 3.6 浮点加减五步法

```text
1. 对阶
2. 尾数相加减
3. 规格化
4. 舍入
5. 判溢出
```

> [!warning] 易错
> 对阶时小阶向大阶看齐，尾数右移。不要让大阶向小阶看齐。

---

## 4. 今日自测

### 必做题

1. 解释 `10000000` 在无符号数、原码、反码、补码中的含义。
2. 用权值法计算 8 位补码 `11101001` 的真值。
3. 写出 8 位补码整数的范围，并解释为什么比原码多一个负数。
4. 判断补码尾数 `1.0110` 是否规格化。
5. 将一个简单十进制数转换为 IEEE754 单精度，至少完整走一遍流程。
6. 写出补码加减法的基本公式。
7. 说明进位和溢出的区别。
8. 写出浮点加减五步法。

### 今日过关标准

- 看到补码机器码能在 20 秒内算出真值。
- 能说清 `10000000` 为什么在补码中是 -128。
- 能独立写出 IEEE754 单精度真值公式。
- 能区分逻辑移位、算术移位、循环移位。
- 能把浮点加减流程背成 5 步。

---

## 5. 晚上 10 分钟复盘

写下：

```text
今天最稳的 3 个点：
1.
2.
3.

今天最容易错的 3 个点：
1.
2.
3.

明天开头必须回看：
1.
2.
```
