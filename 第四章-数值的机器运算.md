# 第四章 数值的机器运算

> [!summary] 章节概览
> 本章讨论数值数据在计算机中实现**算术运算**和**逻辑运算**的方法，以及运算部件的基本结构和设计。核心是将一切运算归结为**加法 + 移位**。

---

## 学习目标

| 层次 | 内容 |
|---|---|
| ==掌握== | 定点补码加减法、原码/补码乘法、原码/补码加减交替除法 |
| ==理解== | 3种溢出检测方法、补码移位与舍入操作、进位产生/传递、浮点加减乘除 |
| ==了解== | 串行/并行加法器、运算器基本结构、浮点协处理器 |

---

## 4.1 基本算术运算的实现

> [!note] 核心思想
> 计算机中最基本的运算单元是**加法器**。加减乘除最终都归结为**加法运算**，因此加法器的速度和效率直接影响 CPU 性能。

### 4.1.1 加法器

#### 1. 全加器（FA）

全加器是最基本的加法单元：

| | 信号 | 说明 |
|---|---|---|
| 输入 | $A_i, B_i, C_{i-1}$ | 两个操作数 + 低位进位 |
| 输出 | $S_i, C_i$ | 本位和 + 向高位进位 |

**逻辑表达式：**

$$
S_i = A_i \oplus B_i \oplus C_{i-1}
$$

$$
C_i = A_iB_i + (A_i \oplus B_i)C_{i-1}
$$

#### 2. 串行加法器 vs 并行加法器

| 类型 | 结构 | 优点 | 缺点 | 应用 |
|---|---|---|---|---|
| 串行加法器 | 1个全加器，逐位串行 | 器件少，成本低 | 速度太慢 | 基本淘汰 |
| 并行加法器 | n个全加器，各位同时运算 | 速度快 | 进位传递是瓶颈 | 主流方案 |

> [!important] 关键问题
> 并行加法器的速度瓶颈在于**进位信号的传递时间**，而非全加器本身的求和延迟。例如 `11...11 + 00...01`，最低位进位会逐位影响至最高位。

---

### 4.1.2 进位的产生与传递

每位进位表达式：

$$
C_i = G_i + P_i \cdot C_{i-1}
$$

| 函数 | 表达式 | 含义 |
|---|---|---|
| 进位产生函数 | $G_i = A_iB_i$ | 本位直接产生进位 |
| 进位传递函数 | $P_i = A_i \oplus B_i$ | 低位进位能否穿透本位 |

**串行进位（行波进位）的延迟：**

- 每级进位延迟 $2t_y$（$t_y$ = 一级与门/或门延迟）
- n位总延迟 = $2nt_y$，**与字长成正比**

---

### 4.1.3 快速进位方式

#### 1. 并行进位（先行进位）

各级进位同时形成：

$$
\begin{aligned}
C_1 &= G_1 + P_1C_0 \\
C_2 &= G_2 + P_2G_1 + P_2P_1C_0 \\
C_3 &= G_3 + P_3G_2 + P_3P_2G_1 + P_3P_2P_1C_0 \\
C_4 &= G_4 + P_4G_3 + P_4P_3G_2 + P_4P_3P_2G_1 + P_4P_3P_2P_1C_0
\end{aligned}
$$

> [!warning] 优缺点
> - ✅ 延迟仅 $2t_y$，与字长无关
> - ❌ 位数增加时表达式急剧膨胀，**全部采用并行进位不现实**

#### 2. 分组并行进位

| 方式 | 结构 | 延迟 |
|---|---|---|
| **单级先行进位**<br>（组内并行，组间串行） | 每4位一组 → CLA电路 | 组内 $2t_y$，组间串行 |
| **多级先行进位**<br>（组内并行，组间并行） | CLA + BCLA 两级 | ==$6t_y$==（16位两级） |

**组进位函数（多级先行）：**

$$
G_i^* = G_4 + P_4G_3 + P_4P_3G_2 + P_4P_3P_2G_1
$$

$$
P_i^* = P_4P_3P_2P_1
$$

---

## 4.2 定点加减运算

### 4.2.1 原码加减运算

1. 操作数取**绝对值**
2. 加法 → 直接相加；减法 → 减数先**变补**（前变补）再相加
3. 有进位 → 结果为正；无进位 → 结果为负，需再变补（**后变补**）
4. 最后加上符号位

### 4.2.2 补码加减运算

> [!tip] 补码的核心优势
> 符号位直接参与运算，减法统一为加法，硬件实现最简单。

**加法公式：**

$$
[X+Y]_{补} = [X]_{补} + [Y]_{补}
$$

**减法公式：**

$$
[X-Y]_{补} = [X]_{补} + [-Y]_{补}
$$

**求机器负数（变补）：**

$$
[-Y]_{补} = [[Y]_{补}]_{变补}
$$

即：**将 $[Y]_{补}$ 连同符号位一起变反，末位加1**。

> [!important] 变补 ≠ 求补码
> - 原码→补码：符号位不变，仅数值位变反+1
> - **变补**：不论正负，符号位和数值位**一起**变反+1

**补码加减规则：**
1. 操作数均用补码表示
2. ==符号位作为数的一部分参加运算==
3. 加法 → 直接相加 | 减法 → 被减数 + 减数的机器负数
4. 结果仍用补码表示

**符号扩展：** 正数填 `0`，负数填 `1`。

---

### 4.2.3 溢出与三种检测方法

> [!note] 何时溢出？
> n+1位定点整数（1位符号），结果不在 $[-2^n, 2^n-1]$ 内即溢出。
> **只有同号数相加才可能溢出**（异号实为相减）。

#### 方法一：单符号位判断

$$
溢出 = \overline{X_s}\;\overline{Y_s}S_s + X_sY_s\overline{S_s}
$$

| $X_s, Y_s$ | $S_s$ | 结果 |
|---|---|---|
| 0, 0（两正） | 0 | 正常 |
| 0, 0（两正） | 1 | ==正溢== |
| 1, 1（两负） | 1 | 正常 |
| 1, 1（两负） | 0 | ==负溢== |

#### 方法二：进位位判断

$$
溢出 = C_s \oplus C_1
$$

| $C_s$ | $C_1$ | 结果 |
|---|---|---|
| 0 | 1 | 正溢 |
| 1 | 0 | 负溢 |
| 0 | 0 / 1 | 1 | 正常 |

- $C_s$：符号位进位
- $C_1$：最高数值位进位

#### 方法三：变形补码（双符号位）

$$
溢出 = S_{s1} \oplus S_{s2}
$$

| $S_{s1}S_{s2}$ | 状态 |
|---|---|
| `00` | 正数，无溢出 |
| `01` | ==正溢== |
| `10` | ==负溢== |
| `11` | 负数，无溢出 |

---

### 4.2.4 补码加减运算的硬件实现

> [!info] 核心部件
> - **加法器 F**：执行加法运算
> - **寄存器 X, Y**：存放操作数
> - **门电路 A, B, C**：控制数据流向
> - 控制信号：$X \rightarrow F$、$Y \rightarrow F$、$F \rightarrow X$
> - 减法时用 $Y \rightarrow F$ + $1 \rightarrow F$ 实现变补

---

## 4.3 移位与舍入操作

### 4.3.1 移位规则

| | 原码 | 补码（正数） | 补码（负数） |
|---|---|---|---|
| 符号位 | 不变 | 不变 | 不变 |
| 左移空出位 | `0` | `0` | ==`0`== |
| 右移空出位 | `0` | `0` | ==`1`== |

> [!tip] 记忆口诀
> 补码负数移位：**左移补0，右移补1**（只有这个特殊，其余都补0）

### 4.3.2 舍入方法

设结果共 p+q 位，只保留 p 位：

| 方法 | 规则 | 特点 |
|---|---|---|
| **(1) 恒舍（切断）** | 多余部分一律舍去 | 最简单，误差最大 |
| **(2) 冯·诺依曼舍入** | 保留部分最低位强制置 `1` | 又称"恒置1法" |
| **(3) 下舍上入** | 看舍去部分最高位：`0`舍 `1`入 | 即"0舍1入" |
| **(4) 查表舍入（ROM舍入）** | 用ROM存放舍入表，查表处理 | 灵活但需额外存储 |

---

## 4.4 定点乘法运算

### 4.4.1 原码一位乘法

**规则：**
1. 操作数取**绝对值**运算
2. 乘数最低位 = `1` → +被乘数；= `0` → +0
3. 部分积与乘数**右移一位**
4. 重复 n 次后，符号位单独处理：$P_s = X_s \oplus Y_s$

**寄存器：** A（部分积，初值0）、B（被乘数）、C（乘数）

> [!example] 例：X = 0.1101，Y = -0.1011
>
> | 步 | A | C | 操作 |
> |---|---|---|---|
> | 初 | 00.0000 | 1011. | |
> | 1 | 00.0110 | ⭢1101 | C₄=1, +|X|, 右移 |
> | 2 | 00.1001 | ⭢1110 | C₄=1, +|X|, 右移 |
> | 3 | 00.0100 | ⭢1111 | C₄=0, +0, 右移 |
> | 4 | 00.1000 | ⭢1111 | C₄=1, +|X|, 右移 |
>
> $P_s = 0 \oplus 1 = 1$，结果：**XXY = -0.10001111**

---

### 4.4.2 补码一位乘法（Booth乘法）

> [!important] 核心区别
> 补码乘法中**符号位参与运算**，且乘数后增加一位附加位 $Y_{n+1}$（初值 `0`）。

**Booth操作规则：**

| $Y_n$ | $Y_{n+1}$ | 操作 |
|---|---|---|
| `0` | `0` | 部分积右移一位 |
| `0` | `1` | 部分积 + [X]补，右移一位 |
| `1` | `0` | 部分积 + [-X]补，右移一位 |
| `1` | `1` | 部分积右移一位 |

**要点：**
- 被乘数和部分积取==双符号位==，乘数取一位符号位
- 移位按==补码右移规则==
- 共 ==n+1 次累加==，==n 次移位==（最后一次不移）

---

### 4.4.3 补码两位乘法

每次处理==两位==，速度提高一倍。

| $Y_{n-1}Y_nY_{n+1}$ | 操作 |
|---|---|
| `000` | +0，右移2位 |
| `001` / `010` | +[X]补，右移2位 |
| `011` | +2[X]补，右移2位 |
| `100` | +2[-X]补，右移2位 |
| `101` / `110` | +[-X]补，右移2位 |
| `111` | +0，右移2位 |

- 被乘数和部分积取==三符号位==
- n为偶数：$\frac{n}{2}+1$ 次累加，$\frac{n}{2}$ 次移位
- n为奇数：最后一次单独移一位

---

## 4.5 定点除法运算

### 4.5.1 原码除法

#### 恢复余数法 → 不恢复余数法

| 方法 | 思路 | 缺陷 |
|---|---|---|
| 比较法 | 先比较大小，再上商 | 需比较电路，硬件代价大 |
| 恢复余数法 | 先减后判，不够减则恢复 | 操作次数不固定，速度慢 |
| ==不恢复余数法（加减交替法）== | 不够减时不恢复，下次加回来 | ✅ 最佳方案 |

**不恢复余数法通式：**

$$
r_{i+1} = 2r_i + (1 - 2Q_i) \times Y
$$

| $Q_i$ | 操作 |
|---|---|
| `1`（够减） | 部分余数左移一位 → 下次**减**除数 |
| `0`（不够减）| 部分余数左移一位 → 下次**加**除数 |

> [!warning] 注意事项
> - 要求 $|被除数| < |除数|$ 且除数 $\neq 0$（防止溢出）
> - 最终余数为负时，必须**恢复一次余数**
> - 最终余数需 $\times 2^{-n}$ 才是真值

---

### 4.5.2 补码除法（加减交替法）

#### 够减判断与上商

| [r_i]补 与 [Y]补 | 上商 |
|---|---|
| 同号 | ==1== |
| 异号 | ==0== |

> [!tip] 商符自动形成
> 第一次上商即为商符，与数值位一致，不需单独处理。

**求新部分余数：**

$$
[r_{i+1}]_{补} = 2[r_i]_{补} + (1 - 2Q_i) \times [Y]_{补}
$$

- 上商 `1` → 左移，**减**除数
- 上商 `0` → 左移，**加**除数

**末位恒置1**：商的最末位强制为 `1`，最大误差 $2^{-n}$。

#### 补码加减交替法总表

| [X]补 与 [Y]补 | [r_i]补 与 [Y]补 | 商 | 新余数操作 |
|---|---|---|---|
| 同号 | 同号（够减） | 1 | $2[r_i]_{补} - [Y]_{补}$ |
| 同号 | 异号（不够减）| 0 | $2[r_i]_{补} + [Y]_{补}$ |
| 异号 | 同号（够减） | 1 | $2[r_i]_{补} - [Y]_{补}$ |
| 异号 | 异号（不够减）| 0 | $2[r_i]_{补} + [Y]_{补}$ |

---

## 4.6 规格化浮点运算

设：$A = M_A \times 2^{E_A}$，$B = M_B \times 2^{E_B}$

### 4.6.1 浮点加减运算 —— 五步法

> [!summary] 运算流程
> **对阶 → 尾数加减 → 规格化 → 舍入 → 溢出判断**

#### (1) 对阶

小阶向大阶看齐：$\Delta E = E_A - E_B$

- $\Delta E > 0$（$E_A > E_B$）：$M_B$ 右移，$E_B+1$，直至阶码相等
- $\Delta E < 0$（$E_A < E_B$）：$M_A$ 右移，$E_A+1$，直至阶码相等

尾数右移后需舍入。

#### (2) 尾数加减

$M_A \pm M_B \rightarrow M_C$，与定点加减法相同（双符号位补码）。

#### (3) 规格化

| 尾数形式 | 状态 | 操作 |
|---|---|---|
| `00.1xx...` / `11.0xx...` | ✅ 已规格化 | 无 |
| `00.0xx...` / `11.1xx...` | 绝对值太小 | ==左规==：尾数左移，阶码减1（可多次）|
| `01.xxx...` / `10.xxx...` | 绝对值>1 | ==右规==：尾数右移，阶码加1（仅一次）|

> [!important] 注意
> `01`/`10` 在定点中是溢出，但在浮点中仅表示需要右规，**并非真正溢出**。

#### (4) 舍入

通常采用恒舍法：无条件丢弃尾数最低位之后的全部数值。

#### (5) 溢出判断

看阶码（补码表示）：

| $[E]_{补}$ | 类型 | 处理 |
|---|---|---|
| `01, xxx...x` | 上溢 | ==中断处理== |
| `10, xxx...x` | 下溢 | 按机器零处理 |

---

### 4.6.2 浮点乘除运算

#### 乘法

$$
(M_A \times 2^{E_A}) \times (M_B \times 2^{E_B}) = (M_A \times M_B) \times 2^{E_A + E_B}
$$

**步骤：**
1. **阶码相加**：移码需减偏移量 $2^n$
   $$[E_A+E_B]_{移} = [E_A]_{移} + [E_B]_{移} - 2^n$$
2. **尾数相乘**：同定点乘法
3. **规格化**：因 $\frac{1}{4} < |M_A M_B| < 1$，只需左规最多一次

#### 除法

$$
(M_A \times 2^{E_A}) \div (M_B \times 2^{E_B}) = (M_A \div M_B) \times 2^{E_A - E_B}
$$

**步骤：**
1. **尾数调整**：若 $|M_A| \geq |M_B|$，$M_A$ 右移一位，$E_A+1$（最多一次）
2. **阶码相减**：移码需加偏移量 $2^n$
3. **尾数相除**：同定点除法，结果必在规格化范围内

---

### 4.6.3 浮点运算器的实现

> [!info] 组成
> 浮点运算器 = **阶码运算部件** + **尾数运算部件** + 溢出判断电路
> 
> 现代计算机常以**协处理器**形式实现（如 80x87），协助主处理器工作。

---

## 4.7 十进制整数的加法运算

### 4.7.1 8421码加法

- 按二进制加 → 若和 > 9 → ==+6 校正==
- 校正函数：$校正 = C_4 + S_4S_3 + S_4S_2$

> [!example] 校正示例
> - $5+8 = 13$：`0101 + 1000 = 1101` → 和 > 9 → `1101 + 0110 = 1 0011`（8421码的13）✅
> - $9+8 = 17$：`1001 + 1000 = 1 0001` → 有进位 → `0001 + 0110 = 0111` → 加上进位 = 17 ✅

### 4.7.2 余3码加法

- 按二进制加 → 有进位 → ==+3 校正==（+0011）；无进位 → ==-3 校正==（+1101）

---

## 4.8 逻辑运算

> [!note] 特点
> 按位进行，==位之间没有进位/借位关系==，比算术运算简单得多。

| 运算 | 符号 | 含义 |
|---|---|---|
| 逻辑非 | $Z_i = \overline{X_i}$ | 按位取反 |
| 逻辑乘（与） | $Z_i = X_i \land Y_i$ | 按位与 |
| 逻辑加（或） | $Z_i = X_i \lor Y_i$ | 按位或 |
| 逻辑异或（按位加）| $Z_i = X_i \oplus Y_i$ | 按位模2和 |

---

## 4.9 运算器组成与实例

### 4.9.1 运算器基本结构

**组成：** ALU + 寄存器组 + 内部总线

**两种前端设计：**
- **带多路选择器**：寄存器通过MUX选通进入ALU，内部总线单向
- **带输入锁存器**：ALU前设锁存器暂存操作数，内部总线双向

**三种总线结构：**

| 结构 | 操作步骤 | 速度 | 复杂度 |
|---|---|---|---|
| 单总线 | 3步 | 慢 | 简单 |
| 双总线 | 2步 | 较快 | 中等 |
| 三总线 | ==1步== | 最快 | 复杂 |

### 4.9.2 ALU 芯片 —— 74181

- 4位ALU，可执行 ==16种算术 + 16种逻辑==运算
- **串行进位**：级联 $C_{out} \rightarrow C_{in}$
- **并行进位**：配合 ==74182== 先行进位部件

### 4.9.3 浮点协处理器 —— 80x87

> [!info] 协处理器模式
> - CPU 执行常规指令
> - 80x87 只执行 **ESC指令（换码指令）**
> - 两者可==并行执行==

**数据格式：**
- 整数：16/32/64位补码
- 压缩十进制：80位（72位有效），原码
- 浮点数：32/64/80位，阶码移码 + 尾数原码

**内部结构：** 总线控制 + 数据接口 + ==浮点运算部件（阶码 + 尾数 + 移位器）==

---


