# 四天复习 Day 2：指令系统与 CPU

> [!summary] 今日主线
> 今天解决“一条指令如何被编码、取出、译码、执行”。  
> 第 3 章回答 **指令长什么样**；第 6 章回答 **CPU 怎样让这条指令真的发生**。

覆盖章节：

- 第 3 章：指令系统
- 第 6 章：中央处理器

---

## 0. 今日时间安排

| 时间块 | 任务 | 产出 |
|---|---|---|
| 80 min | 指令格式、地址码、扩展操作码 | 会做扩展操作码题 |
| 80 min | 寻址方式、堆栈、CALL/RET | 会算 EA，能讲 CALL/JMP/RET |
| 90 min | CPU 组成、寄存器、取指微操作 | 能写取指周期微操作 |
| 90 min | 控制器、时序、微程序 | 能区分微命令/微操作/微指令/微程序 |
| 50 min | 流水线、RISC、自测 | 能讲吞吐率和三类相关 |

---

## 1. 第 3 章：指令系统

### 1.1 指令格式

```text
指令 = 操作码 + 地址码
操作码：做什么
地址码：操作数在哪、结果放哪、下一条去哪
```

地址数减少的本质：

```text
三地址：隐藏下一条指令地址
二地址：隐藏结果地址
一地址：隐藏 ACC
零地址：隐藏堆栈
```

> [!warning] 易错
> 二地址指令结果会覆盖一个源操作数。零地址指令不是没有操作数，而是操作数隐含在堆栈里。

### 1.2 扩展操作码

核心思想：

```text
地址数越多，操作码越短；
地址数越少，操作码越长。
```

解题模板：

```text
当前层可用编码数 - 当前层所需指令数 = 可扩展前缀数
下一层可用编码数 = 可扩展前缀数 × 2^(下一个地址字段位数)

如果某一层：
可用编码数 < 所需指令数
则无法实现。
```

### 1.3 寻址方式

| 寻址方式 | 公式 | 人话理解 |
|---|---|---|
| 立即寻址 | `S = A` | A 本身就是操作数 |
| 直接寻址 | `EA = A` | A 就是真地址 |
| 间接寻址 | `EA = M[A]` | A 是地址的地址 |
| 寄存器寻址 | `S = R[i]` | 操作数在寄存器 |
| 寄存器间接 | `EA = R[i]` | 寄存器中放地址 |
| 变址寻址 | `EA = A + R[x]` | 基准地址 + 下标 |
| 基址寻址 | `EA = R[b] + D` | 基址 + 位移 |
| 相对寻址 | `EA = PC + D` | 当前位置 + 位移 |
| 基址变址 | `EA = R[b] + R[x] + D` | 基址 + 变址 + 位移 |

> [!warning] 易错
> 立即寻址的 A 是操作数，不是地址。只有直接寻址中 `EA = A`。

### 1.4 堆栈

```text
LIFO：后进先出
SP：指向栈顶
```

自底向上生成堆栈：

```text
PUSH A：
SP ← SP - 1
M[SP] ← A

POP A：
A ← M[SP]
SP ← SP + 1
```

零地址指令：

```text
操作数从栈顶弹出
结果压回栈顶
```

### 1.5 `JMP` / `CALL` / `RET`

| 指令 | 作用 |
|---|---|
| `JMP` | 无条件转移，不保存返回地址 |
| `CALL` | 调用子程序，保存返回地址 |
| `RET` | 根据返回地址返回 |

理解：

```text
CALL 要回来，所以必须保存返回地址。
嵌套调用后调用先返回，所以返回地址适合放堆栈。
```

---

## 2. 第 6 章：CPU

### 2.1 CPU 主线

```text
控制器在正确时间发出控制信号，
让数据沿 PC、MAR、MDR、IR、ACC、ALU、主存流动，
最终完成机器指令。
```

### 2.2 寄存器分工

| 寄存器 | 作用 |
|---|---|
| PC | 下一条指令地址 |
| IR | 当前指令 |
| MAR | 主存地址 |
| MDR | 主存和 CPU 之间的数据缓冲 |
| ACC | 累加器，参与运算和保存结果 |
| PSWR | 状态标志和控制标志 |

以 `ADD X` 理解：

```text
PC → MAR
M[MAR] → MDR → IR
IR 中地址码 X → MAR
M[X] → MDR
ACC + MDR → ACC
PSWR 更新
```

### 2.3 时序系统

```text
指令周期
  └── 机器周期
        └── 节拍
              └── 工作脉冲
```

理解：

- 指令周期：完成一条机器指令的总时间。
- 机器周期：完成一个基本操作，如取指、取数、执行。
- 节拍：机器周期内部的分步动作。
- 工作脉冲：真正触发寄存器写入或状态翻转。

> [!warning] 易错
> 机器周期不是指令周期。一条指令通常包含多个机器周期。

### 2.4 取指周期微操作

常见取指公共操作：

```text
(PC) → MAR
Read
M(MAR) → MDR
(MDR) → IR
(PC) + 1 → PC
```

单总线限制：

```text
同一时刻只能一个部件向总线输出；
可以多个部件同时从总线输入。
```

判断：

```text
PC_out, MAR_in          允许
MDR_out, IR_in, Y_in    允许
PC_out, MDR_out         不允许
```

### 2.5 Y 和 Z 为什么存在

单总线一次只能传一个数，但 ALU 通常需要两个操作数。

完成：

```text
ACC ← ACC + MDR
```

要分三步：

```text
ACC_out, Y_in
MDR_out, ALU_add, Z_in
Z_out, ACC_in
```

### 2.6 微程序控制

层级关系：

```text
机器指令：ADD X
    ↓
微程序：一串微指令
    ↓
微指令：一组微命令
    ↓
微命令：控制一个微操作
    ↓
微操作：(PC) → MAR
```

| 概念 | 含义 |
|---|---|
| 微命令 | 控制信号 |
| 微操作 | 控制信号导致的具体动作 |
| 微指令 | 一组微命令 |
| 微程序 | 实现一条机器指令的一串微指令 |
| 控制存储器 CM | 存放微程序 |

> [!warning] 易错
> 机器指令在主存，微指令在控制存储器 CM；机器指令对程序员可见，微指令通常不可见。

### 2.7 流水线

流水线提高的是：

```text
吞吐率 = 单位时间完成的指令数
```

不一定缩短单条指令从开始到结束的总时间。

三类相关：

| 相关类型 | 直觉 |
|---|---|
| 结构相关 | 硬件资源不够用 |
| 数据相关 | 前一条结果还没出来，后一条要用 |
| 控制相关 | 下一条指令地址不确定 |

### 2.8 RISC 为什么适合流水线

```text
指令长度固定 → 取指译码简单
指令格式少 → 控制逻辑规整
寻址方式少 → 执行阶段统一
Load/Store 架构 → 访存集中
多数指令单周期 → 流水段容易均衡
```

核心不是“少”，而是：

```text
简单、规整、容易硬连线控制、容易流水线化
```

---

## 3. 今日自测

1. 二地址指令为什么会覆盖一个源操作数？
2. 扩展操作码题如何判断“能不能设计出来”？
3. 写出 9 种寻址方式的 EA / S 公式。
4. 自底向上堆栈 PUSH/POP 顺序是什么？
5. `CALL` 和 `JMP` 最大区别是什么？
6. PC、MAR、MDR、IR 分别解决什么问题？
7. 写出取指周期公共微操作。
8. 单总线结构中多个 `_out` 为什么冲突？
9. 微命令、微操作、微指令、微程序的层级关系是什么？
10. 流水线提高的是单条指令速度还是吞吐率？

---

## 4. 今日过关标准

- 能完整写出取指周期微操作。
- 能用一句话解释控制器的本质。
- 能独立判断单总线微操作能否并行。
- 能区别机器指令和微指令。
- 能说清 RISC 为什么适合流水线。

---

## 5. 晚上复盘

```text
今天必须背死的 3 条：
1.
2.
3.

今天最容易混的 3 组概念：
1.
2.
3.

明天先回看：
1.
2.
```
